# Halcon DLT（Deep Learning Tool 25.12）功能对照与开发建议

本文基于 MVTec **Deep Learning Tool 25.12** 官方英文文档（`training_manual/` 11 篇 +
`user_manual/` 7 篇 + index）通读整理，逐页对照本项目（DeepMaven）现状，
给出「必要 / 好用」的待开发功能清单、实现要点与优先级，供确认后分批实施。

> 状态标记：✅ 已有 ｜ 🟡 部分具备（有差距） ｜ ❌ 缺失

---

## 一、DLT 功能全景（按页面）

### 1. PROJECTS 项目页
- 最近项目按时间降序；双击 / `OPEN` / `OPEN PROJECT…` / **拖放 `.dltp` 打开**
- 新建项目：7 种方法（分类、异常检测、轴对齐检测、旋转框检测、Deep OCR、语义分割、实例分割），
  名称 / 路径 / 描述
- **从现有数据集（HDICT）创建项目**：校验并修正图像路径（语义分割另有标签图路径）
- 偏好设置里可设「最近项目数量」「启动时打开最近项目」
- **崩溃恢复**：非正常退出后残留项目在页顶列出，可一键 `RECOVER` 后另存

### 2. GALLERY 图库页
- **带自动标注的文件夹导入**：按目录层级（从左 N 级 / 从右 N 级）、反向拼接、分隔符生成类别名，
  可逐个改名；异常检测项目可编辑每类的 good / anomaly 类型
- 无标注导入（单图 / 整个文件夹树）；删除图像（仅移除引用 / **Delete from Filesystem**）
- **标签类别**：新增 / 改名 / 改色 / 删除（"No label" 内置不可删）
- **图像标记（Tags）**：文本 + 颜色，一张图可多个；点标记即追加 / 再点删除；hover 可编辑 / 删除
- **筛选**：快速筛选（按标签类别 / 标记 / 拆分映射）+ 文件名文本筛选 + **自定义筛选规则**
  （条件：名称 / 路径 / 状态 / 评论 / 宽 / 高 / 通道数 / 标记 / 标注数量 / 标注类别 / 异常类型 / 拆分；
  关系：包含、不包含、开头、结尾、正则、`==`、`!=`、`<`、`<=`、`>`、`>=`；支持 AND / OR 组合与括号，
  筛选定义随项目保存）
- 缩略图：尺寸滑杆、**亮度 / 对比度滑杆（仅显示）**、类别名叠加（宽度可调）、
  T / V / E 拆分角标（点击展开缩写）
- **拆分映射区**：点 `T`/`V`/`E`/`Not in split` 直接改图像的子集归属（拆分未训练前）
- 图像评论（自由文本，随项目保存）
- **标签统计对话框**：各类别数量、占比、未标注统计（选中集或整体）

### 3. IMAGE 图像（标注）页
- 导航器：缩放 / 适配 / 平移（滚轮、`Ctrl` + 拖动）；**十字准线**（可开关、透明度可调）；
  **`Shift` 显示像素数据**
- 画布显示：亮度 / 对比度、标注区域不透明度（分割）
- 标注工具：矩形、**旋转框（先拖主轴再定宽）**、多边形、**掩码画笔 / 橡皮**、
  **孔洞（Hole）**、组件（多区域合并为一个实例）、**多边形 ↔ 掩码互转**
- **智能标注**：Bounding Box / Hover & Click（AI 生成候选，无需手工勾轮廓）
- 实例编辑：拖动移动、边角缩放（`Shift` 锁比例）、旋转、**数值表直接改 X / Y / 宽 / 高 / 角度**、
  朝向切换（旋转 180°）
- **多选与批量**：`Shift` 框选 / `Ctrl` 点选 / `Ctrl+A`；**标注复制粘贴（`Ctrl+C/X/V`，
  可跨图、跨实例，甚至跨 DLT 进程；自动补类别）**
- 快捷键：**`1-9` 与若干字母选类别**、方向键移动（`Alt` + 方向键细调）、`Del` 删除、
  `PageUp/Down` 翻图、`Ctrl+PageUp/Down` 首末图、`Ctrl+0/+/-` 缩放适配、`?` 查看快捷键总览
- 类别：hover 编辑 / 删除；语义分割的 **背景类（bg）**（唯一、不可删、可切换、未标注像素归背景）

### 4. REVIEW 检查页
- 展示粒度按任务不同：分类按整图（带类别色块）；检测 / 分割 **按实例分块展示**（与所在图像解耦），
  一张图多个对象就显示多块；旋转框统一以 0° 显示便于比对
- 类别区显示每类数量；筛选：未标注 / 单类 / `Ctrl` 多类 / 清除
- 视图：缩略图尺寸、**纵横比**、**区域边距**、亮度 / 对比度、不透明度（分割）、
  整体顺时针 / 逆时针旋转（旋转框）
- 批量纠正：多选后统一下拉改类（分类可选 "No label"）、批量删除、**批量纠正朝向**

### 5. SPLIT 拆分页
- 多套拆分：新建 / 编辑 / 复制 / 删除 / 切换；默认比例 70:15:15，
  **点 ± 按钮 / 滚轮 / 直接输入数量或百分比**
- **随机种子可复现**；`CREATE EMPTY SPLIT`（先建后配）
- 改映射（`CHANGE SPLIT MAPPING` / `Not in split`）；**拆分一旦用于训练即锁定**
- 拆分备注（随项目保存）；异常检测特例：只有 good 图参与比例、异常图按**绝对数量**放入验证集

### 6. TRAINING 训练页
- **Model Trainings 面板（多套训练设置）**：`CREATE MODEL` 新建、`⋮` 复制 / 删除 / 切换，
  每套设置独立保存参数与结果，便于横向比较同一拆分的不同设置
- SETTINGS：参数分「常用 / 高级（折叠）」；**`EDIT / PREVIEW` 预览预处理后的图像效果**
  - 网络：图像宽 / 高、通道数（3 或 1）、预训练模型（Compact / Enhanced / ResNet-50 / ResNet-18 /
    MobileNetV2 / AlexNet，或**外部模型管理**）、Patch Size（异常）、Alphabet（OCR）
  - 训练：Epochs、Iterations、Batch Size、Learning Rate、**学习率策略（After Epoch → New Value）**、
    Solver（adam / sgd）、Momentum、**Weight Prior（正则）**、Validation Interval、
    Bbox / Class / Mask 头权重、**Freeze Backbone Level**、
    **类权重自定义（滑块 + `BALANCE` / `RESET`，训练前归一化）**
  - 硬件：设备（CPU/GPU）、确定性算法、随机种子
  - **数据增强**：启用开关 + 作用比例（%）、旋转步长、镜像轴（水平 / 垂直 / 两者）、
    亮度、亮度斑、对比度、饱和度、裁剪（分类）、旋转范围、去除像素
- RESULTS：
  - **预处理进度 + 剩余时间**（训练前先预处理全部图像）
  - 训练状态（epoch / iteration / loss）、**实时曲线**：Loss、Top-1 错误率（分类）、
    F1（异常）、Mean-AP（检测 / 实例分割）、Mean-SoAP（旋转框）
  - 每项都给出**曲线读法与调参建议**（发散 → 降学习率 / 加 batch；过拟合 → 加 Weight Prior；
    欠拟合 → 降学习率；训练误差 vs 验证误差对比）
  - **暂停 / 继续 / 重置**；暂停后关掉程序仍可继续；**已完成的训练可通过增加 epochs 继续训练**
  - 开始训练后参数**锁定**（想改参数需新建训练设置）
- 训练产物：`Training-<date>-<time>/` 目录存放 `best_model.hdl` 与
  `dl_preprocess_param.hdict`、`dl_training_params.hdict`、`dl_training_results.hdict`、
  `dl_training_strategies.hdict`

### 7. EVALUATION 评估页
- 左侧训练列表：每个训练显示总指标（分类准确率 / 异常 F1 / 检测 Mean-AP / 语义 Mean-IoU），
  可点 `F1-Score`、`GC-AD Global / Local` 切换查看
- Settings：**选择评估图像集（train / val / test 多选）**、**限制数量（随机子集）**、
  设备、批大小
- Global Measures（随场景）：
  - 准确率、**False / Correct Predictions**、F1、Mean Precision、Mean Recall、
    **Time Elapsed**、**Top-1 Error**、图像总数
  - 检测 / 实例分割：**IoU（默认 75% 阈值）**、Mean-AP、**SoAP（旋转框角度精度）**、
    **误检细分为 5 类**：类别错（FP Class）、无重叠（FP Background）、定位不准（FP Localization）、
    重复（FP Duplicate）、多重错误（FP Multiple）、合计
  - **推理时间测量**：点秒表 → 100 张随机样本、batch=1、取最快的 10 次平均；
    tooltip 显示预处理耗时与总耗时
- **Class Overview 对话框**：逐类指标（可展开 good / anomaly 各类）
- **混淆矩阵**：列为真实、行为预测；对角线绿色为正确；每行 FP 列、每列 FN 行；
  异常检测可切换「逐类」/「good 与 anomaly 合并」视图
- **Images 区**：点单元格筛选对应图像；缩略图 / 大图切换；**按置信度排序**
  （误检高置信、正确低置信往往是标注错误）；`Preprocessed` 对比原图；
  `Heatmap` / `Domain` / **`Guided Grad-CAM`（分类）** 叠加；选中显示
  Name / True Label Class / Prediction + Confidence（可展开多类）
- **修正真实标签**：直接在评估页改 `True Label Class` 并即时重算（会改变 ground truth）
- 异常检测专有：**Score Histogram**（分数分布 + 三条阈值线，可拖动中位阈值），
  **后处理参数对话框**：限制域（画 ROI）、分类阈值、分割阈值、**异常分数容忍度（0~50%）**、
  **最小缺陷尺寸（滤掉小连通域）**
- 暂停 / 重置评估

### 8. EXPORT 导出页
- 左侧选训练（多子模型可分别导出）；展示该训练的评估概览
- **Optimized for inference**（默认开，释放推理不需要的内存）
- **Optimized for AI2 接口**：设备 + 精度 + 批大小；TensorRT 支持 int8；
  int8 / Hailo 需要**校准图像数量**（随机取自训练集）
- `EXPORT MODEL` → 模型 + `dl_preprocess_params.hdict`
- **Generate Report**：导出 HTML 报告，含项目描述与评论、拆分与训练信息、全局评估指标、
  类别概览、混淆矩阵、**good / 假正 / 假负 预测样本图**

### 9. 全局能力
- **撤销 / 重做**：`Ctrl+Z` / `Ctrl+Y`，步数几乎不限（筛选 / 排序等非项目操作不参与）
- **偏好设置**（跨会话）：界面语言、默认项目目录、最近项目数量、启动打开最近项目、
  低刷新率提示、十字准线显示与不透明度、标注区域不透明度、图像亮度 / 对比度、
  滚轮缩放方向、触摸板平移手势、缩略图类别名框宽度、**CPU 线程数**
- **图像基础路径**：无基础路径 / 绝对基础路径 / 相对项目文件夹；**Relocate Images** 一键迁移；
  文件缺失时可用 `BROWSE…` 批量重定位
- 命令行：`--help / --version / --reset_preferences / --add_preferences / --load_preferences /
  --use_preferences`、`-c conf.json`、`-t theme.json`（主题：明暗 + 主色 + 警告 / 错误色）
- **完整快捷键表**（项目 `Ctrl+N/O/W/S`、页面 `Alt+1..8`、图库选择 / 移动、标注类选择 / 移动 /
  复制粘贴 / 翻页 / 缩放、检查页类别筛选）
- 日志：`%USERPROFILE%\AppData\Local\MVTec\DeepLearningTool\logfile-*.log`，
  保留最近 10 个，帮助菜单可直接打开日志目录

---

## 二、本项目现状对照

| 能力域 | 现状 | 差距 |
|--------|------|------|
| 项目管理 | ✅ 新建 / 打开 / 保存 `.mprj`、最近项目、描述 | 🟡 无拖放打开、无崩溃恢复、无自动备份 |
| 图像导入 | ✅ 文件 / 文件夹导入、SHA256 去重、多来源累加；目录层级 → 类别名（左 / 右 N 级、反向、定界符，可逐个改名）、异常检测的类别类型（良好 / 异常）、标注 / 不标注两种导入 | 🟡 无相机采集（抓拍入库） |
| 标签类别 | ✅ 增删改 + 颜色 + 预设色板 | ✅ 基本对齐 |
| 图像标记 | ✅ 文本 + 颜色、多标记、点击追加 / 删除、计数 | ✅ 基本对齐 |
| 筛选 | 🟡 快速筛选（类别 / 标注状态 / 拆分 / 标记）+ 组合选择 | ❌ 无自定义规则（条件 + 关系 + AND/OR）、无按尺寸 / 通道 / 标注数筛选、筛选不随项目保存 |
| 缩略图显示 | ✅ 尺寸档位（88/124/176/352）、Ctrl+滚轮、角标（已标注 / 拆分 / 标记） | ❌ 无亮度 / 对比度调节、无类别名叠加、无纵横比 / 边距 |
| 拆分映射 | ✅ 侧栏点选改划、锁定机制 | 🟡 无「Not in split」显式项、无批量改划 |
| 图像备注 | ✅ 标注页可写备注（sidecar） | 🟡 图库 / 检查页不能直接看 / 改 |
| 标签统计 | 🟡 侧栏类别计数 | ❌ 无统计对话框（占比 / 未标注 / 选中集统计） |
| 标注画布 | ✅ 矩形 / 旋转框 / 多边形、缩放平移、亮度对比度、导航器、标注对象列表 | 🟡 无掩码画笔 / 橡皮、无孔洞、无组件、无多边形↔掩码互转 |
| 标注编辑 | 🟡 拖动 / 缩放 / 旋转滑杆 / 删除 | ❌ 无数值表（X/Y/宽/高/角度直接输入）、无方向键微调、无多选批量改类 / 删除 |
| 标注复制 | ❌ 无 | ❌ 无 Ctrl+C/V、无跨图复制 |
| 智能标注 | ✅ 半自动预标注（SAM 2 / MobileSAM 提示式分割转多边形） | ✅ 对齐 DLT 的 Smart Labeling（思路一致） |
| 快捷键 | ❌ 仅画布 `Del` / `Esc` | ❌ 无快捷键体系（无 QShortcut）、无快捷键总览 |
| 检查页 | ✅ 大卡片复核、详情、按类别 / 标注状态筛选、一键质检 | 🟡 检测 / 分割未按实例分块展示；无纵横比 / 边距 / 亮度对比度；无批量改类 / 批量纠正朝向 |
| 拆分 | ✅ 多套拆分、比例 / 种子 / 分层、产物、数据增强、锁定 | 🟡 无拆分备注、无法创建空拆分后再配 |
| 训练 | ✅ 变体 / 权重、参数（求解器 / 权重衰减 / 动量 / 预热 / 早停 / 种子 / 确定性 / 矩形 / 设备 / workers）、实时曲线、日志、记录回看、多模型对比、断点续训 | 🟡 无「训练设置（setup）」对象（新建 / 复制 / 命名 / 备注）、无暂停 / 继续、无类权重、无参数预览（预处理效果）、无曲线读法提示 |
| 评估 | ✅ 数据集评估：准确率 / 精确率 / 召回率 / F1 / Top-1 错误率 / 推断耗时、混淆矩阵（含误检 / 漏检）、结果缩略图（对错 + 置信度）、筛选 / 翻页、逐类概率、修正真实标签、报告导出 | 🟡 检测未细分 FP（类别错 / 背景 / 定位 / 重复 / 多重）、无 GT 框叠加、无按置信度 / IoU 排序、无实例级视图；❌ 无多子集选择 + 随机子集、无热图 / 预处理对比 / Grad-CAM、异常无分数直方图与阈值调整、无后处理参数 |
| 导出 | ✅ PT / ONNX / TorchScript、针对推断 / 针对 API 优化、导出记录、模型报告（HTML / MD，含拆分 / 指标 / 混淆矩阵） | 🟡 报告缺预测样本图；❌ 无半精度 / 精度选项与校准 |
| 异常检测 | ✅ Anomalib（Padim 等）训练 + `.ckpt` 推理 | 🟡 无阈值 / 分数分布可视化、无最小缺陷尺寸与域限制 |
| 全局 | 🟡 主题 / 主色 / 最近项目用 QSettings 持久化；日志轮转保留 3 个 | ❌ 无撤销 / 重做、无偏好设置页、无日志目录入口、无图像基础路径 / 重定位、无命令行参数 |

---

## 三、开发建议清单

### 第一批：必要项（缺口明显，直接影响日常使用与数据安全）

| # | 功能 | 为什么必要 | 实现要点 |
|---|------|-----------|---------|
| 1 | **撤销 / 重做** | DLT 的核心交互；误删标注、错改类别无法回退是硬伤 | 引入 `QUndoStack`；把「标注增删改、类别增删改、标签 / 标记赋值、拆分映射、图像移除」等改动包成 `QUndoCommand`；`Ctrl+Z` / `Ctrl+Y`；筛选 / 排序等视图操作不入栈 |
| 2 | **快捷键体系 + 快捷键总览** | 标注是高频重复劳动，少一次鼠标就快一分 | `QShortcut` 统一注册：页面 `Alt+1..8`、项目 `Ctrl+N/O/S/W`、图库 `Ctrl+A` / 空格选择 / 方向键移动 / `Del` / `Ctrl+F`；标注页 `1-9` 选类、方向键移动（`Alt` 细调）、`PageUp/Down` 翻图、`Ctrl+0/+/-` 缩放；`F1` 或 `?` 弹出快捷键表 |
| 3 | **标注数值编辑 + 键盘微调 + 多选批量操作** | 精修一个框靠鼠标拖不准 | 标注对象列表加 X / Y / 宽 / 高 / 角度可编辑列；`Shift` 框选 / `Ctrl` 点选 / `Ctrl+A`；选中多个后统一改类、删除、方向键移动 |
| 4 | **掩码（画笔 / 橡皮）标注 + 孔洞 + 多边形↔掩码互转** | 划痕、异物等有机形状用多边形点数爆炸；分割任务的实际刚需 | 画布加 mask 图层（`QImage` 单通道 + 笔刷 / 橡皮 + 笔径滑杆）；孔洞用多边形子路径（奇偶填充）；互转时多边形栅格化 / 掩码轮廓提取（`cv2.findContours`）；落盘仍走 YOLO-Seg 多边形或掩码 |
| 5 | **崩溃恢复 + 自动保存** | `.mprj` 是单文件，程序异常退出会丢一整个标注批次 | 编辑后定时（如 60s）写入 `*.mprj.bak`；启动时检测残留并提示「恢复 / 丢弃」；保存成功后清理 |
| 6 | **图像基础路径 + 重定位** | 数据集换盘 / 换机器后整库失效；工业现场常见 | 项目记录基础路径（无 / 绝对 / 相对项目）；加载时缺失文件批量 `BROWSE…` 重定位并回写；图库角标提示缺失 |
| 7 | **偏好设置页** | 现在设备 / 线程 / 滚动方向等散落在各页，且不持久 | 统一设置对话框（`QSettings` 持久化）：默认项目目录、最近项目数量、启动打开最近项目、CPU 线程数、滚轮缩放方向、十字准线 + 不透明度、标注区域不透明度、图像亮度 / 对比度默认值、主题与主色、日志目录入口 |

### 第二批：好用的专业能力

| # | 功能 | 价值 | 实现要点 |
|---|------|------|---------|
| 8 | **自定义筛选规则** | 大数据集里找「小图 / 单通道 / 未标注 / 某类为 0」等，是质检刚需 | 规则模型（条件 + 关系 + 值 + AND/OR + 括号）+ 规则对话框 + 侧栏快捷入口；规则随项目保存；支持名称 / 路径 / 宽 / 高 / 通道 / 标注数 / 类别 / 标记 / 拆分 / 备注 |
| 9 | **图库 / 检查页显示增强** | 暗光 / 过曝工业图不调对比度看不清 | 把标注页已有的亮度 / 对比度实现提升为共享组件，接到图库与检查页；缩略图可选叠加类别名 |
| 10 | **标签统计对话框** | 快速判断数据是否平衡、还差多少 | 按类别 / 拆分 / 标记统计数量与占比 + 未标注统计；支持仅统计选中集 |
| 11 | **检测评估增强** | 现在只有「误检 / 漏检」，定位不准与重复检出无法区分，评估结论不够用 | FP 细分为类别错 / 无重叠 / 定位不准 / 重复 / 多重；图像叠加 GT（实线）与预测（虚线）框；按置信度 / IoU 排序（找错标注）；实例级 / 图像级双视图 |
| 12 | **评估可视化增强** | 判断「模型到底在看哪里」 | `Preprocessed` 预处理对比视图（letterbox + 归一化还原）；异常检测热图（Anomalib 输出）；分类 Grad-CAM（末层钩子 + 反传） |
| 13 | **异常检测分数直方图 + 阈值 / 后处理** | 异常检测的阈值直接决定误报率，必须可调 | 分数分布直方图 + 阈值线可拖动；最小缺陷尺寸（连通域过滤）、异常分数容忍度（分位数）、域限制 ROI；结果即时重算 |
| 14 | **训练设置（setup）管理 + 备注** | 现在只有一份训练配置，调参对比要靠记忆；DLT 用「多套训练设置」解决 | `TrainingSetup` 列表（名称 / 参数 / 结果 / 备注，新建 / 复制 / 删除 / 切换）；训练页左侧面板列出，每套独立保存 |
| 15 | **训练暂停 / 继续** | 长训练占用机器时能暂停很有用 | 与子进程训练循环配合：暂停时挂起数据加载（或到 epoch 边界停），继续时恢复；已完成训练可加 epochs 继续 |
| 16 | **评估多子集选择 + 随机子集** | 现在只能选一个拆分的单个子集，且上限取的是「前 N 张」 | 多选 train / val / test；`限制数量` 改为随机抽样（固定种子可复现） |
| 17 | **报告增强** | 报告要能直接给客户 / 同事看 | 嵌入 good / 假正 / 假负 样本缩略图（Base64 内联，保持单文件）；补类别概览、项目备注、训练参数摘要 |
| 18 | **日志管理 + 帮助菜单入口** | 出问题时能自己取日志 | 轮转保留最近 N 个日志（现在 3 个同步改为可配）、帮助菜单「打开日志目录」 |
| 19 | **导出精度选项** | 部署端对体积 / 速度敏感 | ONNX / TorchScript 加 `half`（fp16）选项；TensorRT 相关选项标注为「需安装对应运行时」 |

### 第三批：锦上添花 / 依赖后端

| # | 功能 | 说明 |
|---|------|------|
| 20 | 十字准线 + 像素值显示 | 画布叠加十字线（可开关 / 透明）、`Shift` 显示光标处像素值 |
| 21 | 拖放打开项目 / 图片 | 主窗口接受 `.mprj` / 图片 / 文件夹拖放 |
| 22 | 命令行参数 | `--project <path>`、`--reset_preferences`、`--theme <json>` |
| 23 | OOD 检测（分布外识别） | Ultralytics 无原生支持，可用分类模型特征 + 马氏距离 / 能量分数自研，需评估精度与工作量 |
| 24 | 类别权重（类不平衡） | Ultralytics 训练接口未暴露逐类损失权重；分类可尝试加权采样，检测暂不可行 |
| 25 | 训练参数预览（EDIT / PREVIEW） | 训练前预览「按当前增强 / 缩放参数处理后的图像」，现在只有离线增强预览，可扩展到训练参数 |

### 明确不做（后端不支持或与产品定位不符）

| 项 | 原因 |
|----|------|
| HDICT / HDL 互操作、`.dltp` 兼容 | 属于 HALCON 生态，本项目走 Ultralytics + `.mprj` 自有格式 |
| AI2 接口导出（OpenVINO / TensorRT / Hailo 全流程） | 需要额外运行时与硬件，工程量远超收益；仅保留 ONNX 体现的部署路径 |
| GC-AD 全局 / 局部子网、Patch Size | Anomalib 无对应模型；现有 Padim / PatchCore 等已覆盖结构异常 |
| 语义分割「背景类」导出为 HALCON 掩码 | 依赖 HALCON 数据格式；本项目按 YOLO 生态输出 |
| 许可证管理、语言切换（多语言包）、DirectX/渲染相关偏好 | 与自有产品形态无关 |

---

## 四、建议实施方案（若确认）

**第 1 阶段（第一批 1~4）**：撤销 / 重做 → 快捷键体系 → 标注数值编辑与批量操作 →
掩码 / 孔洞标注。这一组直接改变日常标注体验，且改动集中在标注页与命令层，风险可控。

**第 2 阶段（第一批 5~7）**：崩溃恢复 + 自动保存 → 基础路径与重定位 → 偏好设置页。
这一组是数据安全与可用性，改动分散但独立。

**第 3 阶段（第二批 8~13）**：筛选规则 → 显示增强 → 标签统计 → 检测评估增强 →
评估可视化 → 异常阈值 / 后处理。

**第 4 阶段（第二批 14~19 + 第三批按需）**：训练设置管理 / 暂停继续 → 评估多子集 →
报告增强 → 日志 / 导出精度 → 十字准线等小项。

每阶段结束按既有习惯：offscreen 自检 + 真实项目只读冒烟 + `session records.md` 记录，
并在 `开发文档.md` 增补对应章节。
