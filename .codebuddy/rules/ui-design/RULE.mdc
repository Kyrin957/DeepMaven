---
description: DeepMaven 界面开发规范（容器化布局、参数框定宽、滚轮不改值、DPI 适配、设计令牌）；改动 src/views 或 src/app.py 的界面代码时应用
alwaysApply: false
enabled: true
---

# DeepMaven 界面开发规范

技术栈：PySide6 6.11 + QFluentWidgets 1.11（Fluent Design）。界面代码位于
`src/views/`，设计系统基座位于 `src/views/ui/`（路径均相对仓库根）。

> 本规范用于根治历史上反复出现的问题：**控件重叠**、**输入框横贯整屏**、
> **滚轮误改参数**、**高分屏错位**。新增 / 修改界面代码时逐条对照。

---

## 1. 布局：容器化 + 可换行，禁止"单行塞满"

- **禁止**把 3 个以上控件直接塞进一个 `QHBoxLayout` 作为工具条 / 操作栏 /
  筛选栏 / 翻页行。`QHBoxLayout` 空间不足时**不会换行**，只会把子项压到最小：
  - 紧的会**互相覆盖**（标注页工具条文字与指示条重叠）；
  - 宽的会把**文字截断**（图库筛选栏「标签: 全部」被压成「签: 全」、
    评估页「Grad-CAM」被压到 35px）。

  **这两种现象都不允许出现**——商业软件里任何一处文字被截断都是缺陷。
- 上述行一律用 `FlowContainer` + `ToolGroup`：

  ```python
  from src.views.ui import FlowContainer, ToolGroup

  container = FlowContainer(
      card, spacing=T.SPACE_MD,
      margins=(T.SPACE_XL, T.SPACE_MD, T.SPACE_XL, T.SPACE_MD),
  )
  card_layout.addWidget(container)
  container.add(ToolGroup(label, combo, parent=card))   # 一组：整体折行
  container.add(ToolGroup(btn_a, btn_b, parent=card))
  container.add(single_button)                          # 单个：独立折行
  ```

- 每个 `ToolGroup` 是一个"逻辑分组"，整体折行、不被拆散；
  需要独立折行的单个控件，直接 `container.add(控件)`。
- **控件不得被压到小于其 `sizeHint().width()`**（否则文字必被截断）。
  放不下就折行。确需给宽度时用**下限** `setMinimumWidth`，
  **不要**用 `setFixedWidth` 去"将就"一个过窄的值
  （历史故障：把「|◀」固定成 38px 后文字被截）。
- 若在最窄可用宽度下**整行都放不下**（极窄侧栏 / 极高缩放），
  才降级为**只显示图标**（`setIcon` + 空文本 + `setToolTip`），
  而不是让文字被截断。
- **整组显隐**用 `group.setVisible(...)`，不要逐个隐藏组内控件
  （`FlowLayout` 会自动跳过隐藏分组，不留空位）。

## 2. 参数输入框：必须有界，禁止拉伸满行

- **禁止** `QFormLayout.addRow(label, spin)` 后放任输入框 `Expanding`
  （历史故障：参数框横贯整屏，视觉上像"一条长线"）。
- 数值输入框用 `tokens.field_width(控件, 字符数)` 定宽，按**字体度量**计算，
  高分屏 / 字体变化时自动适配：

  ```python
  spin.setFixedWidth(T.field_width(spin, T.STEPPER_CHARS))       # 常规
  spin.setFixedWidth(T.field_width(spin, T.STEPPER_CHARS_WIDE))  # 带后缀 / 大数值
  ```

- 成组的参数用 `ParamGroup`（有边框的模块化容器）承载，内部用
  `add_field()`（单列）或 `add_grid(fields, columns=2)`（多列）。
- 路径 / 备注 / 名称一类的**文本**输入可以撑满一行；**数值**输入必须定宽。
- 行内排布：`标签(定宽) + 控件(定宽) + addStretch(1)`，左对齐。

## 3. 滚轮：参数框永不改值

- **参数类**数值输入框一律用 `SafeSpinBox` / `SafeDoubleSpinBox`
  （`from src.views.ui import SafeSpinBox`），滚轮不再改值，且滚动会
  转交给所在滚动区。
- `src/app.py` 已安装全局 `WheelGuard`，覆盖所有历史代码与弹窗中的输入框。
  **不要移除**：`app.installEventFilter(WheelGuard(app))`。
- **显示类**控件（亮度 / 对比度 / 缩略图尺寸等）用 `Slider`，不受限制。
  若确有输入框需要保留滚轮改值，显式设置属性：

  ```python
  setattr(widget, "_allow_wheel_edit", True)   # 仅限显示类，并在注释里说明理由
  ```

## 4. 尺寸与间距：只用 tokens

- 禁止在页面里写死间距 / 宽度数字。统一从 `src/views/ui/tokens.py` 取值：

  | 用途 | 令牌 |
  |------|------|
  | 间距（4 的倍数） | `SPACE_XXS/XS/SM/MD/LG/XL/XXL` |
  | 卡片内边距 | `CARD_PAD_H` / `CARD_PAD_V` |
  | 侧栏宽度 | `SIDE_W_NARROW` / `SIDE_W` / `SIDE_W_WIDE` |
  | 数值框宽度 | `field_width(...)` + `STEPPER_CHARS*` |
  | 圆角 | `RADIUS_SM/MD/LG` |

- 侧栏一律用 `data_widgets.side_column(*cards, width=tokens.SIDE_W)`，
  内容超高时由侧栏自身滚动。

## 5. 高度与缩放：只给"提示值"，绝不写显式最小/固定高度

高度上有两条硬约束，违反任一条都会让**窗口被撑得比屏幕还高、顶部标题栏
（最小化 / 关闭按钮）被顶出可视区**——这是本项目反复复发的故障，
详见 `开发文档.md` §4.4 / §4.8。

1. **页面不得向窗口传导最小尺寸**：注册页面时的 `QSizePolicy.Ignored`
   必须保留（`main_window.py::_add_page`），**不要移除**。

2. **禁止用 `setMinimumHeight` / `setMinimumSize` / `setFixedHeight`
   表达"内容希望的高度"**。Qt 的 `qSmartMinSize()` 会**无视 `Ignored` 策略**
   强行采用显式最小值，于是最小高度重新沿布局层层累加到窗口。

   高度需求一律用**提示值**表达：重写 `sizeHint()` / `minimumSizeHint()` /
   `heightForWidth()`，并给尺寸策略打开 `HeightForWidth`。
   可变行数的工具条/操作栏请直接用 **`FlowContainer`**
   （`src/views/ui/flow_layout.py`），它已按此实现；
   **不要再给它的宿主写任何最小高度**（用 `--no-sync` 之外的任何写法都不行）。

3. **窗口最小尺寸必须 ≤ 屏幕可用区域**。已由
   `main_window._apply_screen_bounded_minimum()` 统一 clamp（高分屏
   125% / 150% 会把可用逻辑高度压到低于设计最小尺寸）。不要改回
   硬编码的绝对最小值。

4. 宽度不要用固定像素去"对齐文字"，用 `field_width()`；
   自绘控件读 `devicePixelRatio`（参考 `widgets/charts.py`）。

5. 大块内容 / 长列表自带滚动（`ScrollArea`、`data_widgets.side_column`）。
   窗口变矮时应当是**滚动**，而不是把控件挤到重叠。

## 6. 视觉层级与分组

- 层级固定为：**页面 > 卡片（CardWidget）> 分组容器（ParamGroup）> 字段行（FieldRow）**。
- 同一层级的间距必须一致（用 `SPACE_*` 令牌），避免"东一块西一块"。
- 卡片标题用 `StrongBodyLabel`，分组标题用加粗 `CaptionLabel`（`ParamGroup` 已处理）。
- 分隔用 `tool_separator()`，不要用空 `QLabel("|")`。

## 7. 主题与配色

- 常规控件交给 QFluentWidgets 主题，**不要**写死明暗色 `setStyleSheet`
  背景 / 前景（暗色主题下会失效或刺眼）。
- 自绘控件的中性色从 tokens 取（`CANVAS_BG` / `GROUP_BORDER` / `GROUP_BG`）。
- 类别 / 图表配色从 `utils/constants.py` 的 `SPLIT_COLORS` / `PLOT_COLORS` 取。

## 8. 自检要求

界面改动完成后必须做离屏自检（不要只靠"看起来对"）：

```powershell
# 在仓库根（工作区根）执行
$env:QT_QPA_PLATFORM="offscreen"
.venv\Scripts\python.exe <你的自检脚本>
```

自检至少覆盖：

1. 在 1080×680 / 1280×800 / 1600×900（外加一个更矮的尺寸，如 1080×600）下，
   遍历 `FlowLayout` 的分组，断言**分组几何矩形互不相交**，
   且宿主高度 ≥ `heightForWidth(width)`（不被裁剪）；
2. 向参数输入框发送滚轮事件，断言**数值不变**且所在滚动区**发生了滚动**；
3. 构建完整 `MainWindow`，确认所有导航页均可构造、无异常；
4. **§4.4 回归断言（必做）**：
   - `win.minimumSizeHint().height() <= 屏幕可用高度`
   - `win.minimumSize()` 不超出屏幕可用区域
   - 页面内可变行数容器（`FlowContainer`）的 `minimumHeight() == 0`
     —— 一旦不为 0，说明有人注入了显式最小尺寸，故障即将复发。
5. **横向压缩断言（必做）**：遍历各页内可渲染文字的控件
   （按钮 / 下拉 / 勾选 / **未开启换行**的标签），断言
   `控件宽 + 1 >= 控件.sizeHint().width()`。
   不满足即说明该行在被压缩、文字即将被截断。
   文本输入框与数字框允许更窄（内容内部滚动），排除在外。

## 9. 参考实现

- 工具条容器化 + 换行：`src/views/annotate_tab.py::_build_toolbar`
- 参数分组容器 + 紧凑步进器：`src/views/train_tab.py::_build_param_card`
- 基座：`src/views/ui/{tokens,flow_layout,wheel_guard,containers}.py`
- 背景与取舍：`开发文档.md` §4.5 / §4.6
