# 基于PySide6的深度学习缺陷检测系统——开发方案与实施步骤


## 一、项目概述

### 1.1 目标定位

参考Halcon Deep Learning Tool的设计理念，开发一款面向工业缺陷检测（划痕、异物等）的桌面应用，实现从项目管理、数据管理、模型训练到模型导出的完整工作流。

### 1.2 核心功能模块

| 模块 | 功能描述 |
|------|---------|
| **项目管理** | 创建/打开/保存单文件 `.mprj` 项目，管理项目结构与最近项目；**自动保存 + 崩溃恢复**（`.mprj.bak`）、**图像基础路径与缺失重定位** |
| **数据管理** | 图片导入与去重、类别管理、数据质检、分层划分（异常检测按类别分配）；**自定义筛选规则**（条件 + 且/或组合）、**标签统计**（类别 / 拆分 / 标记 + 占比）、**显示增强**（亮度 / 对比度 / 类别名叠加，只影响显示） |
| **图像标注** | 内置标注器（矩形/多边形/**掩码画笔 + 孔洞**）、多选与批量编辑、**数值编辑与方向键微调**、半自动预标注（YOLO 预标注 + SAM 细化）、标注格式互通、十字准线与像素值显示 |
| **全局能力** | **撤销 / 重做**（`Ctrl+Z` / `Ctrl+Y`，覆盖标注、类别、图像标记、拆分映射等）、**快捷键体系 + `F1` 总览**、**偏好设置**（项目目录、最近项目数、CPU 线程、滚轮方向、十字准线、区域不透明度、默认亮度对比度、自动保存间隔）、`Alt+1..8` 页面切换、拖放打开项目 / 图片、日志目录入口 |
| **预训练模型导入** | 导入 YOLO 系列预训练权重，支持模型选择与配置（已并入「模型训练」页） |
| **数据拆分** | **一个项目可创建多套拆分**（新建 / 复制 / 重命名 / 删除 / 切换），每套独立保存比例、随机种子、分层方式与产物，训练时按需选用 |
| **模型训练** | 模型变体与权重导入、参数配置（求解器 / 权重衰减 / 早停 / 随机种子 / 数据增强 / 设备）、拆分选择、训练启动 / **轮边界暂停与继续** / 中止、**追加轮数继续训练**（断点续训）、**实时折线图（Loss 与评价指标）**、训练记录回看、**多模型横向对比**、断点续训；**训练设置（setup）管理**（命名保存多套参数、一键切换对比，随项目保存）、**训练参数预览**（letterbox + 增强示意拼图）、**类别权重**（按训练集频次平衡 / 重置，参考用） |
| **模型评估** | **数据集评估**：在带真实标签的拆分 / 目录上统计准确率、精确率、召回率、F1、推断耗时，绘制**混淆矩阵**与**结果缩略图**（对错边框 + 置信度），右栏可查看图像详情与逐类概率、修正真实标签；「推理」分页保留图片 / 视频 / 相机检测与报告导出 |
| **模型导出** | 选择训练产物 → 优化方向（针对推断 / 针对 API）+ 格式与尺寸 → 导出 PT / ONNX / TorchScript；**生成模型报告**（自包含 HTML / Markdown：模型信息、数据拆分、训练与评估指标、混淆矩阵）；保留历次导出记录 |
| **异常检测** | 基于 Anomalib 的无监督异常检测（仅需正常样本），作为训练/评估的任务类型之一 |


## 二、技术选型

### 2.1 核心框架

| 组件 | 技术选型 | 说明 |
|------|---------|------|
| GUI框架 | **PySide6** | Qt for Python官方绑定，功能完善、跨平台 |
| 深度学习框架 | **PyTorch + Ultralytics** | YOLO系列模型训练与推理的首选方案 |
| 目标检测模型 | **YOLOv11/YOLOv26** | 工业缺陷检测主流算法，支持检测、分割等任务 |
| 图像处理 | **OpenCV-Python** | 图像读取、预处理、标注可视化 |
| 项目存储 | **单文件 .mprj 容器** | 自研单文件格式，将项目信息、数据集、训练记录与模型产物统一打包（详见 6.4 节） |
| 模型导出 | **PT/ONNX/TorchScript** | 跨平台模型部署格式 |
| 数据增强 | **Albumentations** | 主流增强库，Ultralytics 官方集成；支持几何/颜色/噪声增强 |
| 分层划分 | **自研分层抽样** | 按「类别组合」分层后按比例分配，避免小类别整体落进同一子集 |
| 图像标注 | **PySide6 QGraphicsView（自研）** | 内置标注器，与项目数据、类别体系深度耦合，无外部协议风险 |
| 半自动标注 | **SAM 2 / MobileSAM** | 提示式分割出掩码并转多边形，与 YOLO 预标注配合使用 |
| 数据质检 | **自研（Pillow + NumPy + OpenCV）** | 感知哈希查重、模糊/曝光检测、YOLO 标签校验，无需额外依赖 |
| 异常检测 | **Anomalib** | 无监督异常检测（仅需正常样本），补齐 Halcon DLT 的对应能力 |

### 2.2 开发环境

- Python 3.14+
- PySide6 >= 6.11
- PyTorch （CUDA可选）
- Ultralytics 

### 2.3 可选增强

- **QFluentWidgets**：Fluent Design风格组件库，提升界面美观度
- **Matplotlib**：训练曲线可视化（暂未引入，训练曲线由 `views/widgets/charts.py::LineChart` 自绘，避免重依赖）

### 2.4 开源资源选型与协议

| 环节 | 选用资源 | 协议 | 集成方式 |
|------|---------|------|---------|
| 训练/推理骨架 | Ultralytics YOLO | AGPL-3.0 | pip 依赖（已用） |
| 数据增强 | Albumentations | MIT | pip 依赖（已安装） |
| 分层划分 | 自研（按类别组合分组抽样） | — | 无额外依赖 |
| 数据质检 | 自研（Pillow + NumPy + OpenCV） | 随依赖 | 感知哈希查重 / 模糊 / 曝光 / 标签校验，无额外依赖 |
| 内置标注器 | PySide6 QGraphicsView | — | 自研，无外部协议风险 |
| 半自动预标注 | SAM 2 / MobileSAM | Apache-2.0 | pip / onnxruntime |
| 外部标注工具 | X-AnyLabeling | GPL-3.0 | ⚠️ 仅做标注文件互通，**不链接其代码** |
| 外部标注平台 | CVAT | MIT | 独立部署 + 标注文件互通 |
| 外部标注平台 | Label Studio | AGPL-3.0 | ⚠️ 独立部署，不链接其代码 |
| 异常检测 | Anomalib | Apache-2.0 | pip 依赖（已安装） |
| 数据集管理 | FiftyOne | Apache-2.0 | ⚠️ 底层依赖 MongoDB，**不嵌入**，仅借鉴其质检思路 |

**协议结论：** 本项目定位为内部使用 / 自身开源，Ultralytics 的 AGPL-3.0 不构成障碍；若未来需闭源对外分发，须购买 Ultralytics Enterprise 授权。X-AnyLabeling（GPL-3.0）与 Label Studio（AGPL-3.0）只允许以「独立进程 + 文件交换」方式配合使用，禁止链接进本项目代码。

**依赖安装节奏：** Albumentations 已随阶段二D 安装；Anomalib 已随阶段七安装；SAM 权重（`mobile_sam.pt`）与多数 Anomalib 骨干权重由对应框架在首次使用时自动下载（**离线环境**可关闭 Anomalib 预训练权重）。服务层对上述依赖采用懒加载，未安装时对应功能入口给出提示并降级。数据质检（阶段二A）与分层划分（阶段二D）基于已装依赖自研实现，不引入新依赖。


## 三、软件架构设计

### 3.1 整体架构（MVVM模式）

MVVM架构设计：

```
src/
├── models/          # Model层 - 数据模型与状态
│   ├── project.py       # 项目数据模型（完整数据容器）
│   ├── project_file.py  # 项目归档文件索引与缺陷类别定义
│   ├── dataset.py       # 数据集模型
│   └── training.py      # 训练/评估/导出配置模型
├── viewmodels/      # ViewModel层 - 业务逻辑
│   ├── project_vm.py
│   ├── dataset_vm.py
│   ├── model_vm.py
│   ├── train_vm.py
│   ├── evaluate_vm.py
│   └── export_vm.py
├── views/           # View层 - UI界面
│   ├── main_window.py
│   ├── base_page.py       # 导航页基类（可滚动卡片布局）
│   ├── data_widgets.py    # 数据四页共用面板（概览/类别/筛选）
│   ├── widgets/
│   │   ├── thumbnail_grid.py    # 缩略图网格（卡片绘制 + 已标注角标）
│   │   ├── annotation_canvas.py # 标注画布（矩形/多边形 + 亮度对比度）
│   │   ├── navigator.py         # 图像导航器（缩略总览 + 视口框）
│   │   └── charts.py            # 环形图 / 条形图 / 图例
│   ├── project_tab.py
│   ├── gallery_tab.py     # 图库页
│   ├── annotate_tab.py    # 图像标注页
│   ├── review_tab.py      # 标注检查页
│   ├── split_tab.py       # 数据拆分页
│   ├── model_tab.py
│   ├── train_tab.py
│   ├── evaluate_tab.py
│   └── export_tab.py
├── services/        # Service层 - 外部服务
│   ├── project_format.py   # 单文件 .mprj 容器编解码
│   ├── project_service.py  # 项目创建/打开/保存与归档文件管理
│   ├── yolo_service.py     # YOLO训练/推理封装
│   ├── dataset_service.py  # 数据集处理
│   └── export_service.py   # 模型导出
└── utils/           # 工具类
    ├── logger.py
    ├── config.py
    └── constants.py
```

### 3.2 各层职责

- **Model层**：定义数据结构（项目信息、数据集元数据、训练参数等）
- **ViewModel层**：处理业务逻辑，连接Model和View，响应UI事件
- **View层**：PySide6界面组件，响应用户操作，展示数据
- **Service层**：封装项目存储（`.mprj` 容器）、YOLO训练/推理、数据集处理、模型导出等核心功能

### 3.3 数据与训练模块目标结构（随阶段落地）

在 3.1 现有结构基础上扩展，✅ 表示已完成：

```
views/
├── data_tab.py                 # ✅ 子页容器：导入 / 类别 / 质检 / 划分 / 增强
├── data_pages.py               # ✅ 数据子页（导入 / 类别 / 质检 / 划分 / 增强）
├── annotate_tab.py             # ✅ 图像标注页
└── widgets/
    ├── annotation_canvas.py    # ✅ QGraphicsView 标注画布
    └── thumbnail_grid.py       # ✅ 缩略图网格
viewmodels/
├── category_vm.py              # ✅ 类别管理
└── annotate_vm.py              # ✅ 标注逻辑（含半自动预标注）
services/
├── category_service.py         # ✅ 类别 CRUD / 排序 / 颜色 / 导入导出
├── quality_service.py          # ✅ 重复 / 模糊 / 曝光 / 标签校验
├── annotation_service.py       # ✅ 标注读写与格式转换（YOLO / COCO / VOC）
├── autolabel_service.py        # ✅ YOLO 预标注 + SAM 分割
├── train_service.py            # ✅ 训练子进程管理（QProcess）
├── train_worker.py             # ✅ YOLO 训练子进程入口（JSON 事件流）
├── inference_service.py        # ✅ 图片 / 视频检测推理与结果解析
├── report_service.py           # ✅ 检测报告导出（CSV / Excel）
├── anomalib_service.py         # ✅ 异常检测（模型构建 / 数据模块 / 推理）
└── anomaly_worker.py           # ✅ 异常检测子进程入口
models/
└── annotation.py               # ✅ 标注数据模型（框 / 多边形 / 分类）
utils/
└── workers.py                  # ✅ 通用后台任务线程（FunctionWorker）
```


## 四、界面布局设计

参考已有系统的功能布局，建议采用**左侧导航栏+右侧内容区**的经典布局：

### 4.1 导航标签页

参照 Halcon DLT 的「图库 → 标注 → 检查 → 拆分」数据链路，数据环节拆为四个独立导航页：

| 标签页 | 核心功能 |
|-------|---------|
| **项目管理** | 新建/打开/保存 `.mprj` 项目、项目信息展示、最近项目列表、**可恢复项目**（自动备份一键恢复 / 忽略）、**图像位置**（基础路径 + 缺失重定位） |
| **图库** | 图片导入（文件夹 / 多选 / 拖拽 + 子文件夹标签）、数据集概览、标签类别管理、缩略图网格浏览与**多选筛选**（标签 / 数据集拆分 / 标记下拉多选，Ctrl + 滚轮缩放缩略图，四档尺寸）、**自定义筛选规则**（11 个字段 × 7 种关系，且 / 或组合，实时预览命中数，随项目保存）、**标签统计弹窗**（类别 / 拆分 / 标记数量与占比 + 标注覆盖，可切「全部 / 选中」）、**显示增强条**（亮度 / 对比度 / 类别名叠加 + 宽度，只影响显示、不改图片）、`Ctrl+A` 全选 / `Del` 移除 |
| **图像标注** | 单张标注工作台：绘制工具条（浏览 / 矩形 / 多边形 / **掩码**）、**孔洞工具**、掩码画笔与橡皮（可生成轮廓、多边形转掩码）、缩略图导航（Ctrl + 滚轮缩放）、导航器、亮度/对比度、**标注对象列表（多选）**、**编辑选中**（类别 / X / Y / 宽 / 高 / 旋转）、备注、十字准线与像素值 |
| **标注检查** | 大卡片缩略图复核（Ctrl + 滚轮缩放）、已选择图像详情、按类别/标注状态筛选、一键数据质检（重复/模糊/曝光/标签） |
| **数据拆分** | 左侧拆分列表（新建 / 复制 / 删除、切换当前拆分）+ 拆分设置（名称、比例、随机种子、分层；**异常检测改为良好 / 异常 × 训练 / 验证 / 测试的数量与百分比分配**）+ 拆分概览；右侧类别分布预览、拆分产物。**一个项目可有多套拆分**，各自保存产物与统计 |
| **模型训练** | 左侧：项目信息 / 模型与权重 / 开始训练 / 训练记录；中央「设置 / 结果 / 对比」三页——设置含**训练设置（setup）管理**（下拉 + 新建 / 复制 / 删除 + 名称 / 备注，切换即回放整套参数）、**拆分选择**与占比、模型与权重、训练参数（求解器、权重衰减、动量、学习率预热、早停、随机种子、确定性、矩形训练、设备）、**训练参数预览**（letterbox + 增强示意拼图）、数据增强与**类别权重**（平衡 / 重置 + 每类数量与权重）；结果含实时指标卡、**Loss 与评价指标折线图**、指标对比、训练日志；**对比页可按勾选叠加多次训练的曲线并列出指标对照**；模型管理页已并入本页 |
| **模型评估** | 左侧：评估配置（模型权重 / 最近训练 / **数据拆分 + 图像集多选（训练 / 验证 / 测试）+ 随机抽样与种子** / 设备 / 阈值 / 图片上限）+ 数据概览（正确·错误环形图）；中央：综合指标（错误预测、准确率、Top-1 错误率、F1、平均精确率 / 召回率、推断与已用时间）、**误检细分**（类别错 / 无重叠 / 定位不准 / 重复 / 多重错误 + 合计）、**混淆矩阵**（行 = 真实、列 = 预测，点击单元格筛选）、**结果缩略图**（对错边框 + 置信度，可按全部 / 正确 / 错误与类别筛选、**排序（置信度 / IoU / 错误优先）**、**整图 / 实例两种粒度**、翻页）；右栏：图像详情（图像集、真实类别可修正、预测类别 / 置信度、逐类概率、**原图 / 预测图 / GT 叠加**）+ 类别指标表；「推理」分页保留单图 / 视频 / 相机检测 |
| **模型导出** | 左栏：导出配置（训练记录 / 权重、格式、目录、尺寸与 Opset）+ 导出进度；中央：**模型概览**（变体 / 尺寸 / 设备 / Epoch / 最佳指标 / 模型大小）、**数据拆分**（环形图 + 明细）、**评估结果**（指标 + 环形图）；右栏：**导出模型**（针对推断 / 针对 API 优化（互斥）+ **半精度 FP16** + 导出按钮）、**生成报告**（HTML / Markdown，含**预测样本图**）、**导出记录** |

数据四页共用左侧信息栏组件（数据集概览 / 标签类别 / 浏览筛选），保证交互一致。

> 异常检测（Anomalib）不单独设导航页，作为「模型训练 / 模型评估」内的任务类型选项复用同一页面。

### 4.2 界面风格

推荐使用**QFluentWidgets**实现Fluent Design风格，支持亮暗主题切换。

### 4.3 界面文案规范（不放说明性文字）

界面不写功能说明与操作指引，文字只用于**标识**与**反馈**：

| 位置 | 规则 | 示例 |
|------|------|------|
| 控件标签 | 名词 / 短语，≤ 6 字 | 「图像来源」「跳过重复图像」 |
| 空状态 | ≤ 8 字短语，不写操作指引 | 「尚未导入图像」「暂无标记」「未选择」 |
| 卡片 / 面板说明句 | 不写；确需解释的参数放 tooltip（≤ 20 字） | tooltip「留空则使用官方预训练权重」 |
| 状态行 / 通知 | 只讲结果与必要原因，≤ 20 字 | 「已旋转 -7.5°」「未安装 Albumentations」 |
| 确认弹窗 | 标题＝动作，正文一句话讲后果 | 「只移出图库，本地文件不会被删除。」 |

禁止：把「点击…即可…」「…时…」写成整句放在界面上；「（需安装 X）」「（可选）」这类括注。
细节信息写进日志、tooltip 或本文档。

### 4.4 窗口尺寸与最小高度约束

窗口纵向结构：标题栏 48px（浮动）→ 菜单栏 → 内容区（导航栏 + 导航页）→ 状态栏。
窗口最小尺寸为 `setMinimumSize(1080, 680)`，**任何页面的内容都不得把窗口的最小尺寸顶大**：

| 约束 | 做法 | 违反后果 |
|------|------|---------|
| 导航页不向窗口传导最小尺寸 | 注册页面时把尺寸策略设为 `QSizePolicy.Ignored`（见 `main_window.py::_add_page`） | 页面内容的最小高度沿布局层层累加（标注 / 评估页可达 900~1000px），超过屏幕可用高度后，放大时窗口被撑得比屏幕还高，顶部标题栏被顶出可视区 |
| 侧栏（竖排卡片）必须可滚动 | 统一用 `views/data_widgets.py::side_column()`，内容装在 `QScrollArea` 内 | 竖排 5 张卡片即有 ~1000px 最小高度，直接顶高整页与窗口 |
| 页面内部大块内容自带滚动 | 画布 / 列表 / 结果视图用 `ScrollArea` 或 `QScrollArea` 包裹 | 窗口变矮时内容被裁掉且无法到达 |

> 排查方法：`window.minimumSizeHint().height()` 若大于屏幕 `availableGeometry()` 高度，放大即会越界。
> 新增页面或侧栏时按上表检查，勿用 `setMinimumHeight` 表达「希望的最小高度」。

### 4.5 界面设计系统（容器化布局与控件规范）

**要解决的问题**：此前界面尺寸与样式全靠各页面自写，导致四类反复出现的故障——
控件重叠、参数框横贯整屏、滚轮误改参数、高分屏下错位。根因是**缺少一个设计系统层**：

| 根因 | 表现 |
|------|------|
| 无设计令牌 | 尺寸 / 间距是散落在 20+ 文件的魔法数字，改一处不影响别处 |
| 单行横排且不换行 | `QHBoxLayout` 塞满控件；`SegmentedWidget` 不随压缩收缩、`ComboBox` 设了最小宽度，宽度不足时 Qt **无法换行只能重叠**（标注页工具条曾出现文字与指示条重叠） |
| `QFormLayout` + `Expanding` | `addRow(label, spin)` 让输入框拉满整行，视觉上像"一条长线" |
| 无滚轮策略 | Qt `QAbstractSpinBox` 默认悬停即改值，误触后不知原值 |
| 固定像素 | 非整数缩放（125% / 150%）下字体变宽，写死宽度装不下内容 |

**基座**位于 `src/views/ui/`：

| 模块 | 作用 |
|------|------|
| `tokens.py` | 设计令牌（间距 / 侧栏宽度 / 圆角）+ `field_width()`：按**字体度量**计算数值框宽度，随缩放与字体自动适配 |
| `flow_layout.py` | `FlowLayout` 可换行流式布局；并按当前宽度回写宿主最小高度，避免折行后被压扁裁剪 |
| `wheel_guard.py` | `WheelGuard`（装在 `QApplication` 上的全局滚轮防护）+ `SafeSpinBox` / `SafeDoubleSpinBox` |
| `containers.py` | `ToolGroup`（工具分组，整体换行）/ `ParamGroup`（有边框的模块化参数容器）/ `FieldRow` |

**强制规则**（完整版见 `.codebuddy/rules/ui-design/RULE.mdc`）：

1. 工具条 / 操作栏用 `FlowLayout` + `ToolGroup` 分组，宽度不足时**整组换行**，禁止把多个控件直接塞进单个 `QHBoxLayout`；分组显隐用 `group.setVisible()`（`FlowLayout` 跳过隐藏分组）。
2. 数值输入框必须用 `tokens.field_width()` **定宽**，禁止被拉伸填满整行；成组参数用 `ParamGroup.add_field()` / `add_grid()`；文本类（路径 / 备注）可撑满一行。
3. 参数类输入框一律 `SafeSpinBox` / `SafeDoubleSpinBox`，**滚轮永不改值**且滚动转交给所在滚动区；显示类控件（亮度 / 对比度 / 缩略图尺寸）走 `Slider` 不受限。全局 `WheelGuard` 不得移除。
4. 尺寸 / 间距 / 圆角只用 `tokens.py` 的令牌；侧栏用 `side_column(*cards, width=tokens.SIDE_W*)`。
5. 禁止给承载可变行数内容的容器写死高度；不要用固定像素去"对齐文字"。
6. 视觉层级固定为 **页面 > 卡片 > 分组容器 > 字段行**；同层间距一致。
7. 常规控件交给 QFluentWidgets 主题，不写死明暗主题色；自绘控件用 tokens 的中性色。

**已落地的参考实现**：`views/annotate_tab.py::_build_toolbar`（工具条 6 个分组、
整组显隐）、`views/train_tab.py::_build_param_card`（优化器与设备 / 超参数 /
训练选项 三个容器 + 两列紧凑步进器）。

**验证方式**：1080×680 / 1280×800 / 1600×900 三种宽度离屏遍历 `FlowLayout` 分组，
断言分组矩形互不相交且宿主高度 ≥ `heightForWidth(width)`；向参数框发送滚轮事件，
断言数值不变而所在滚动区发生滚动。

### 4.6 规则限定与多端共享

CodeBuddy 的规则分两级，**只有项目级随 git 共享**：

| 层级 | 位置 | 是否随 git 共享 |
|------|------|----------------|
| 项目规则 | 代码库内 `.codebuddy/rules/<规则名>/RULE.mdc` | ✅ 随仓库提交，多端 / 多人共享 |
| 用户规则 | 用户目录（`~/.codebuddy`） | ❌ 仅本机全局生效 |

> 注意两点：① 项目规则要求**每条规则是一个含 `RULE.mdc` 的文件夹**，不是扁平的
> `xxx.mdc`；② 规则只在会话开始时载入，**新增 / 修改规则后需新建会话**才生效。
>
> 另有一个无条件全文载入的入口：仓库根的 `CODEBUDDY.md`（纯 Markdown、无元数据，
> 兼容 `AGENTS.md`），适合放"必须永远遵守"的少量硬约束。

**本项目落地清单**（均已纳入 git）：

| 文件 | 内容 | 载入方式 |
|------|------|---------|
| `CODEBUDDY.md` | 项目概览 + 6 条界面**硬约束** + 指路 | 每次会话无条件全文载入 |
| `.codebuddy/rules/ui-design/RULE.mdc` | 界面开发规范 9 节 + 自检方法（详细版） | 智能体按需载入（`alwaysApply: false`，靠 `description` 触发） |
| `.codebuddy/.gitignore` | 只提交 `rules/`，忽略其余本地产物 | — |
| `开发文档.md` §4.5 / §4.6 | 规范正文与共享机制说明 | 随仓库阅读 |

**多端协作约定**：规则是普通文本文件，**共享完全依赖 git**，没有云端自动同步。

> **实测结论（务必遵守）**：CodeBuddy 的「项目规则」只从**工作区根目录**读取
> `.codebuddy/rules`。因此本项目**工作区根必须是 `DeepMaven/`**（即 git 仓库根）。
> 若把工作区开在它的上一层（`Deep Maven/`），仓库内的 `RULE.mdc` 与
> `CODEBUDDY.md` **都不会被加载**——实测中只有放在工作区根的那一份会被注册。
> 基于此，**仓库外的副本已删除**，仓库内那份是唯一事实来源。

在每台机器上：

1. 把 IDE 工作区直接打开到 **`DeepMaven/`**。父目录下的 `Image/`、`Test/`
   如需使用，用绝对路径引用即可；
2. 规则改动提交到仓库（`git add .codebuddy/rules CODEBUDDY.md 开发文档.md`），
   推到远端后其他机器 `git pull` **并新建一次会话**即生效；
3. 换机后如规则未生效：先确认工作区根目录是不是 `DeepMaven/`，再新建一次会话；
4. 需要确认当前**实际加载了哪条规则**时，直接读取规则列表即可（会返回规则的
   绝对路径与 `scope`；不在列表里的规则说明没被识别）。

### 4.7 界面规范落地范围（全部页面与弹窗）

§4.5 的规范已铺开到**全部导航页与全部弹窗**，不是只改样例页：

| 范围 | 文件 | 落地内容 |
|------|------|---------|
| 基座 | `views/ui/{tokens,flow_layout,wheel_guard,containers}.py` | 令牌、可换行布局、滚轮防护、容器化组件 |
| 共享组件 | `base_page.py`、`data_widgets.py`、`gallery_widgets.py` | 侧栏宽度/间距/图标按钮全部改走令牌；`side_column` 默认宽度取 `SIDE_W` |
| 导航页 | `annotate_tab`、`train_tab`、`project_tab`、`gallery_tab`、`review_tab`、`split_tab`、`evaluate_tab`、`export_tab`、`main_window` | ① 工具条改 `FlowLayout`+`ToolGroup`；② 数值框改 `SafeSpinBox`/`SafeDoubleSpinBox` 并**定宽**；③ 间距/宽度/圆角一律取令牌；④ 主题色不写死 |
| 弹窗 | 12 个对话框 | 数值框定宽 + 安全输入框 + 令牌化间距；`new_project_dialog` 的自绘面板描边改取 tokens |

**几个有代表性的修正**

| 位置 | 问题 | 处理 |
|------|------|------|
| 标注页工具条 / 预标注栏 | 15+ 控件塞单行，窄窗口下文字与指示条重叠 | 拆 6 个 `ToolGroup`，`FlowLayout` 整组换行；显隐改整组切换 |
| 训练页训练参数 | `QFormLayout` 把参数框拉成横贯整栏的"长线" | 拆「优化器与设备 / 超参数 / 训练选项」三个 `ParamGroup`，超参数用两列定宽步进器 |
| 评估页 / 导出页 / 弹窗数值框 | 输入框拉伸满行 | `tokens.field_width()` 定宽（按字体度量算，随 DPI 自适配） |
| 拆分页异常检测数量/百分比 | 侧栏仅 ~260px，6 列数值框 | 新增 `STEPPER_W_TIGHT` / `STEPPER_W_TIGHT_PCT` 紧凑令牌 |
| 全部数值输入框 | 悬停滚轮即改值、且不知原值 | 全局 `WheelGuard` + `Safe*SpinBox`；滚轮改交给所在滚动区，显示类 `Slider` 不受影响 |
| `main_window` | 最小尺寸 1080/680 与 `constants.py` 各写一份 | 改为引用 `WINDOW_MIN_WIDTH/HEIGHT` |

**验证**（offscreen，全部通过）

- 8 个导航页 × 3 种窗口宽度（1080×680 / 1280×800 / 1600×900）：`FlowLayout` 分组矩形互不相交、宿主高度 ≥ `heightForWidth()`，**问题 0 处**；
- 真实控件抽查：滚轮掠过参数框数值不变，同时所在滚动区正常滚动；
- 11 个弹窗全部可构建无异常；
- 整仓 `compileall` 通过。


## 五、分步实施计划

> 阶段二拆分为 A/B/C/D 四个子阶段（导入与类别 → 图像标注 → 半自动预标注 → 划分与增强），阶段四为训练闭环。已完成项以 ✅ 标注。

### 第一阶段：基础框架搭建（第1-2周）✅ 已完成

**目标**：搭建可运行的应用骨架

1. **环境搭建**
   - 安装Python 3.14+、PySide6、PyTorch、Ultralytics
   - 配置虚拟环境，编写requirements.txt

2. **项目骨架**
   - 按MVVM架构创建目录结构
   - 实现主窗口（FluentWindow，无边框窗口）和左侧导航栏
   - 实现信号槽机制和事件处理

3. **项目管理模块（基础）**
   - 定义项目数据模型（项目名称、创建时间、路径、模型类型等）
   - 实现新建项目、打开项目、保存项目功能
   - 将项目元数据与文件统一打包为单文件 `.mprj`（加密清单 + ZIP 载荷）

### 第二阶段 A：数据导入与类别管理（第3周）✅ 已完成

**目标**：把原始图片变成「有类别体系、可管理」的数据集

1. **图片导入**
   - 文件夹 / 多选文件 / 拖拽导入 ✅
   - 导入弹窗（对齐 Halcon DLT 的「导入并标注图像」）：**文件夹层级 → 类别名**
     （左侧（顶层）N 级 / 右侧（底层）N 级、**按相反顺序构建**、类别名称定界符）、
     SHA256 去重开关；底部二选一「导入并标注图像 / 导入图像时不进行标注」✅
   - **类别类型**（异常检测项目）：每个类别可设为「良好 / 异常」（存 `ClassDef.kind`），
     良好与异常下都可以有多个类别 ✅
   - **子文件夹 → 类别**：树形展示所选文件夹结构（含各目录图片数），可勾选要导入的子集；
     每个含图文件夹自动对应一个类别名（默认按层级规则生成，**可双击改名**）；
     **类别名留空的文件夹下图片即「无标签」** ✅
   - 多来源图库：多次导入累加（新图追加到末尾），顺序由项目内 `image_order` 清单固化；
     `image_excludes` 记录移出项 ✅
   - 自动配对 YOLO 标签（images/ + labels/）✅
   - 导入去重：相同内容不重复入库（SHA256）✅
   - 相机采集（OpenCV VideoCapture 抓拍入库）— 待补

2. **类别管理**
   - 类别增删改与排序、颜色，顺序即标签 id ✅
   - 类别导入导出（classes.txt / data.yaml names）✅
   - 类别分布统计图 — 待补（暂以列表展示）

3. **数据质检**
   - 重复图片检测（感知哈希）✅
   - 模糊 / 曝光异常检测 ✅
   - 空标签、越界框、缺失 / 孤立标签检查 ✅
   - 尺寸异常检测 — 待补

### 第二阶段 B：图像标注（第4周）✅ 已完成

**目标**：内置标注器，形成标注闭环

1. **标注画布（QGraphicsView 自研）**
   - 矩形框（检测）、多边形（分割）✅
   - 滚轮缩放、中键平移、选择与删除（Delete 键）✅
   - 缩略图网格导航与标注状态标记 ✅
   - 分类标签、撤销重做、复制上一张 — 待补

2. **标注格式互通** ✅
   - 导入/导出 YOLO txt、COCO json、Pascal VOC xml
   - 兼容 X-AnyLabeling / CVAT 产物（仅文件交换，不链接其代码）

3. **标注质检** ✅
   - 空标签、越界框、缺失 / 孤立标签（见数据管理页「质检」子页）

### 第二阶段 C：半自动预标注（第5周）✅ 已完成

**目标**：降低标注工作量

1. **YOLO 预标注** ✅：用预训练 / 已训练权重批量生成候选框；默认只填充未标注图片，不覆盖人工标注
2. **SAM 细化** ✅：框提示 → SAM（`mobile_sam.pt`，首次使用时自动下载）出掩码 → 转多边形
3. 全程在后台线程执行并显示进度 ✅

### 第二阶段 D：数据集划分（第6周）✅ 已完成

**目标**：产出可直接训练的 YOLO 数据集

1. **分层划分**
   - 按「类别组合」分层抽样，避免小类别整体落进同一子集 ✅
   - 划分比例与随机种子可复现 ✅
   - 落盘为 YOLO 结构、生成 data.yaml ✅
   - **一个项目可创建多套拆分**（新建 / 复制 / 重命名 / 删除 / 切换），
     每套拆分独立保存比例、种子、产物目录与 train/val/test 统计；
     被训练使用过的拆分自动锁定比例 ✅
   - 验证集为空时提前拦截并提示（后端对空 val 会直接报错）✅
   - **异常检测按类别分配**：良好 / 异常各自指定训练 / 验证 / 测试数量，
     训练集只放良好图（见 §6.19）✅
   - 固定数量 / 手动指定归属 — 待补

2. **数据增强**
   - 只保留**训练期在线增强**（Ultralytics 参数 + 训练页预览），与 Halcon DLT 一致；
     拆分页原有的「离线增强」（geometric pipeline 落盘 + 网格预览）
     与 `services/augment_service.py` 已移除 — 两处重复的增强入口容易误用，
     且离线产物不参与训练，只增加磁盘与理解成本

### 第三阶段：预训练模型导入（第7周）✅ 已完成

**目标**：实现预训练模型的导入与管理

1. **模型选择**
   - 下拉菜单选择 YOLO 模型变体（YOLO11 / YOLO26 n/s/m/l/x）✅
   - 后台探测并展示模型信息：任务类型、类别、参数量、网络层数 ✅

2. **权重导入**
   - 官方预训练权重：缺失时由 Ultralytics 自动下载 ✅
   - 自定义预训练权重（.pt 文件选择）并校验存在性 ✅

### 第四阶段：模型训练（第8-10周）✅ 已完成

**目标**：实现完整的模型训练工作流

1. **训练参数配置** ✅
   - 任务类型（由项目类型决定）、模型变体、epochs、batch、学习率、求解器、
     权重衰减、动量、学习率预热、imgsz、早停、随机种子、确定性算法、
     矩形训练、单类别、缓存、断点续训、设备（auto 解析为具体 GPU / CPU）
   - 在线数据增强：水平/垂直翻转、旋转、缩放、平移、色调/饱和度/亮度、马赛克、混合
   - 数据增强参数与训练参数一并随项目保存

2. **训练执行** ✅
   - 独立子进程隔离（见 `services/train_service.py`），训练崩溃不影响 GUI
   - 子进程统一 UTF-8 输出（中文 Windows 的 GBK 控制台会让 Anomalib/Lightning
     的 rich 输出抛 UnicodeEncodeError 中断训练）
   - 设备解析见 `utils/device.py`：`auto` → 有 CUDA 用 `"0"`，否则 `cpu`；
     异常检测路径同样下发 `--device` 并转为 Lightning 的 `accelerator/devices`
   - 启动 / 强制终止；断点续训（`resume` 开关）
   - 暂停 — 待补（Ultralytics 无原生暂停语义）

3. **训练监控** ✅
   - 进度条 + 当前 Epoch 实时更新
   - 指标卡：轮次 / 迭代 / 学习率 / 训练与验证损失 / 已用时 / 预计剩余 / 最佳指标
   - **实时折线图**（`views/widgets/charts.py::LineChart`）：Loss（训练 + 验证）与
     评价指标（mAP50 / mAP50-95 / Precision / Recall / Top1 / Top5 / AUROC），
     鼠标悬停显示该轮各序列读数
   - 指标对比表（上一轮 / 最新）、训练日志实时输出
   - GPU 显存监控 — 待补

4. **训练日志与产物** ✅
   - best.pt / last.pt / results.csv 自动归档进 `.mprj`
   - 训练记录（时间 / 最佳指标 / 产物路径）随项目保存，点击记录可回看该次曲线

### 第五阶段：模型评估（第11周）✅ 已完成

**目标**：实现模型加载与缺陷检测

1. **推理引擎** ✅
   - 加载训练好的 YOLO 权重；权重为 `.ckpt` 时自动走 Anomalib 异常检测分支
   - 支持图片检测、视频逐帧检测、相机实时检测
   - 置信度与 IOU 阈值调节、设备选择
   - 后台线程执行，可随时停止
   - 检测结果图片导出 — 待补

2. **结果展示** ✅
   - 检测框叠加图显示
   - 类别 / 置信度 / 坐标表格
   - 检测结果统计（耗时、目标数量、帧数；异常检测为异常张数）

2b. **数据集评估（带真实标签）** ✅
   - 评估集来自项目内任意拆分的训练 / 验证 / 测试子集，也可直接指定目录
   - 分类按「目录名 = 真实类别」比对并保留逐类概率；检测 / 分割按「同类别 + IoU」
     贪心匹配统计 TP / FP / FN（矩阵含「误检」行与「漏检」列）；异常检测按
     normal / abnormal 目录比对判定
   - 指标：错误预测数、准确率、Top-1 错误率、平均精确率 / 召回率、F1、
     平均推断时间、已用时间；逐类给出样本数 / 精确率 / 召回率 / F1
   - 混淆矩阵可点击筛选；结果缩略图带对错边框与置信度，支持筛选与翻页；
     右栏可修正真实标签并即时重算；评估明细可导出 CSV / Excel

3. **结果导出** ✅
   - 检测报告导出（CSV / Excel）

### 第六阶段：模型导出（第12周）✅ 已完成

**目标**：实现训练后模型的导出

1. **格式支持** ✅
   - PyTorch（.pt）、ONNX、TorchScript

2. **导出配置** ✅
   - 模型来源：项目内任意一次训练产出的 `best.pt`，也可手动选择权重
   - 优化方向：**针对推断进行优化**（固定尺寸 + 图精简）、**针对 API 接口进行优化**（动态尺寸）
   - 导出目录、图像尺寸、ONNX Opset（按格式白名单传参，避免格式不支持的参数报错）
   - 后台执行 + 进度显示；产物自动移动到指定导出目录，并记录进「导出记录」

3. **模型报告** ✅
   - 一键生成自包含报告（HTML / Markdown）：项目与模型信息、数据拆分、
     评估指标与逐类指标、混淆矩阵、导出信息
   - HTML 内联样式与 SVG 环形图，浏览器打开即可直接打印成 PDF

### 第七阶段：异常检测（第13周）✅ 已完成

**目标**：基于 Anomalib 的无监督异常检测

1. 仅需正常样本训练（`normal/` 与 `abnormal/` 目录约定），输出异常分数与判定 ✅
2. 作为「模型训练 / 模型评估」的任务类型（anomaly）接入，复用同一页面 ✅
3. 独立 `anomalib_service.py` 与 `anomaly_worker.py` 子进程 ✅
4. 离线环境可关闭骨干预训练权重（`--no-pretrained`，默认自动回退）✅
5. 导出 ONNX — 待补

### 第八阶段：测试与打包（第14周）

**目标**：完善测试并进行应用打包

1. **功能测试**
   - 各模块功能完整性测试
   - 异常处理测试
   - 跨平台兼容性测试（Windows/Linux/macOS）

2. **性能优化**
   - 大数据集加载优化
   - GPU推理加速
   - 多线程处理

3. **应用打包**
   - 使用PyInstaller打包为可执行文件
   - 使用pyside6-deploy部署


## 六、关键实现要点

### 6.1 YOLO训练服务封装

```python
# services/yolo_service.py
from ultralytics import YOLO

class YOLOService:
    def __init__(self, model_name="yolov8n.pt"):
        self.model = YOLO(model_name)
    
    def train(self, data_yaml, epochs, batch, imgsz, lr):
        results = self.model.train(
            data=data_yaml,
            epochs=epochs,
            batch=batch,
            imgsz=imgsz,
            lr0=lr
        )
        return results
    
    def export(self, format="onnx"):
        self.model.export(format=format)
```

### 6.2 数据集结构要求

YOLO格式数据集结构：
```
dataset/
├── images/
│   ├── train/
│   └── val/
├── labels/
│   ├── train/
│   └── val/
└── data.yaml
```

### 6.3 多线程处理

训练和推理为耗时操作，需使用**QThread**或**QRunnable**在后台执行，避免UI卡顿。

### 6.4 项目文件格式（.mprj）

项目持久化采用**自研单文件格式**（实现见 `src/services/project_format.py`），整体布局：

```
偏移     大小    字段
0        6      Magic: b"DMJPRJ"
6        2      FormatVersion: uint16
8        2      Flags: uint16（bit0 = manifest 已加密）
10       4      Reserved: uint32 = 0
14       4      ManifestLen: uint32
18       N      Manifest 密文（JSON, UTF-8）
18+N     ...    ZIP 归档块
```

| 机制 | 说明 |
|------|------|
| 魔数 | `DMJPRJ`，通用解压/识别工具直接判非法 |
| 清单加密 | Manifest 采用 XOR 流式加密，密钥由固定盐 + 魔数 + 版本派生 |
| 条目名混淆 | ZIP 条目名为内容哈希 ID（`f_<sha256[:16]>.bin`），真实路径只存在于加密清单中 |
| 完整性校验 | 每条文件记录携带 SHA256，容器另存 ZIP 载荷哈希，打开时逐项校验，可发现篡改或损坏 |
| 版本守卫 | 不支持的更高版本直接拒绝打开 |

项目信息、数据集索引、训练记录与模型产物均统一打包进该文件，不再使用 SQLite。

### 6.5 多拆分与多模型（横向比较）

参照 Halcon DLT：**一个项目内可以有多套数据拆分与多个训练模型**，便于横向比较。

```
Project
├─ dataset          图库（图片清单 / 类别统计，项目级唯一）
├─ splits[]         多套拆分：名称、比例、种子、分层、产物目录、data.yaml、train/val/test 统计、锁定标记
├─ active_split_id  当前选中的拆分
├─ training         TrainingConfig：split_id / split_name / data_yaml + 全部超参
│  └─ history[]     训练记录（= 模型）：时间、拆分、模型、最佳指标/轮次、loss、results.csv、best.pt
└─ …
```

要点：

| 环节 | 行为 |
|------|------|
| 拆分列表 | `models/split.py::Split`；`DatasetViewModel` 提供 `splits_ready / add_split / duplicate_split / rename_split / remove_split / select_split`，列表与设置随当前拆分联动 |
| 兼容旧项目 | `Project.ensure_splits()` 在读取项目时把旧的单拆分字段（`Dataset.split_*`、`params.split_name`、`data_yaml`）迁移成一套拆分 |
| 产物隔离 | 每套拆分的产物目录 = 项目同级 `<拆分名>/`，`data.yaml` 记入该拆分；磁盘目录删除拆分时保留 |
| 训练归属 | 训练页「设置 → 数据集拆分」下拉选择拆分，写入 `TrainingConfig.split_id/split_name/data_yaml`；训练完成后该拆分 `locked=True`（比例只读），记录里保存拆分名 |
| 横向对比 | 训练页「对比」页：勾选多个训练记录 → **按 runs 的 results.csv 叠加 Loss 与主指标曲线**，并列出「拆分类 / 模型 / 最佳指标 / 最佳轮次 / 最终 Loss」对照表；`TrainViewModel.run_curves()` 负责读取与缓存 |
| 单机可用性 | 曲线仍为自绘（`views/widgets/charts.py::LineChart`），不引入 Matplotlib |

### 6.6 评估指标与混淆矩阵

评估逻辑集中在 `services/evaluation_service.py`，界面只消费结构化结果：

| 环节 | 实现 |
|------|------|
| 评估集 | `collect_samples()`：子目录名即真实类别（分类 / 异常）；检测族用 `attach_boxes()` 从 `labels/<stem>.txt` 读真实框（兼容 4 值 / 多边形 / 旋转框，取外接矩形） |
| 逐图推理 | `evaluate()` 逐张 `model.predict()`：分类取 `probs` 作逐类概率；检测族取 `boxes` 并与真实框做**同类别 + IoU 贪心匹配**得 TP / FP / FN；同时把 `result.plot()` 落盘，供右栏「预测图」查看 |
| 混淆矩阵 | 行 = 真实、列 = 预测；检测族额外一行「误检」（无真实标签的预测）、一列「漏检」（未匹配的真实标签），与 Ultralytics 口径一致 |
| 指标 | 由矩阵汇总：逐类 TP / FP / FN → 精确率 / 召回率 / F1，宏平均，加上图级准确率、Top-1 错误率、平均推断时间、已用时间 |
| 交互 | 点击矩阵单元格按「真实 → 预测」筛选缩略图；右栏修正真实标签后 `update_true_label()` 立即重算矩阵与指标 |
| 界面组件 | `ConfusionMatrixView`（自绘：对角线绿、错误红、误检 / 漏检橙、可点击）、缩略图评估标记（对错边框 + 左上对错徽标 + 底部置信度条）、`PieChart` 环形概览、`BarChart` 逐类概率 |

### 6.7 模型导出与模型报告

| 环节 | 实现 |
|------|------|
| 模型来源 | 导出页列出项目 `training.history` 中每次训练产出的 `best_weights`，选中即切换导出对象；也可手动选择 `.pt` |
| 优化方向 | `ExportConfig.apply_options()`：针对推断 → `simplify=True`（图精简）；针对 API → `dynamic=True`（动态尺寸），两项可同时勾选 |
| 参数白名单 | `export_service._FORMAT_ARGS`：ONNX 传 `imgsz / opset / dynamic / simplify`，TorchScript / PT 只传 `imgsz` —— Ultralytics 会拒绝对该格式无意义的参数 |
| 导出记录 | 产物路径 / 大小 / 时间写入 `ExportConfig.history`（随项目保存），右栏可回看并打开所在目录 |
| 模型报告 | `report_service.ModelReport`：`ExportViewModel.report_context()` 组装上下文 → `build_html()` / `build_markdown()`；HTML 内联 CSS 与 SVG 环形图（拆分、正确 / 错误），含逐类指标与混淆矩阵表，离线可看、浏览器直接打印成 PDF |


### 6.8 编辑体验：撤销 / 重做、快捷键与掩码标注

参照 Halcon DLT 的高频交互补齐，落地在 `utils/history.py`、`views/shortcuts.py`
与 `views/widgets/annotation_canvas.py`：

| 能力 | 实现 |
|------|------|
| 撤销 / 重做 | `utils/history.py::CommandStack`（进程级单例）：各 ViewModel 在改动前用**快照闭包**登记一步（`push(label, undo, redo, key)`），相同 `key` 的相邻改动自动合并（拖动、连续输入不会产生几十步）；`Ctrl+Z` / `Ctrl+Y`；标注改动立即落盘，保证与磁盘一致 |
| 标注快照 | `AnnotateViewModel.snapshot_items()` 取当前图片标注的字典快照；`_restore_items()` 恢复（必要时先切回该图片），因此撤销可跨图片生效 |
| 快捷键体系 | `views/shortcuts.py::ShortcutManager` 统一注册（页面级用 `WidgetWithChildrenShortcut`，全局用 `WindowShortcut`）并登记到 `F1` 总览；画布内的数字键 / 方向键由 `AnnotationCanvas.keyPressEvent` 处理，避免与输入框冲突 |
| 标注快捷键 | `1-9` 选类别、方向键移动（`Alt` 细调 / `Shift` 大步）、`Del` 删除、`Ctrl+A` 全选、`Ctrl+点击` 加选、`Shift+拖动` 框选、`PageUp/Down` 翻图、`Ctrl+PageUp/Down` 首末、`Ctrl+0` 适应窗口、右键 / 双击闭合多边形 |
| 多选与批量 | 画布维护 `_selection` 索引列表，与右侧标注列表双向同步；支持批量改类别、批量删除、整组拖动与旋转 |
| 数值编辑 | 右侧「编辑选中」卡：类别下拉 + X / Y / 宽 / 高（像素）+ 旋转角，选择变化时回填；`set_geometry()` 对多边形按包围盒做等比缩放 + 平移，对矩形直接写两点 |
| 掩码标注 | 画笔 / 橡皮在图片尺寸的 `QImage` 图层上涂抹；`mask_polygons()` 用 `cv2.findContours(RETR_CCOMP)` 取外轮廓与孔洞，`utils/geometry.merge_holes()` 以**零宽桥**把孔洞并入外轮廓，得到训练端（YOLO 多边形）可用的单条闭合轮廓；`rasterize_selected()` 支持多边形回填掩码继续精修 |
| 孔洞工具 | 多边形模式下勾选「孔洞」后画出的多边形并入当前选中实例（同样走桥接），因此「带孔区域」在面积上被正确挖掉 |

### 6.9 数据安全与偏好设置

| 能力 | 实现 |
|------|------|
| 自动保存 | `ProjectService.autosave()` 把当前项目重新打包写入 `<项目>.mprj.bak`，**不改变**项目的「未保存」标记；`ProjectViewModel` 用 `QTimer` 按偏好间隔（默认 60s，0 关闭）触发，仅在项目 `dirty` 时写入 |
| 崩溃恢复 | `ProjectService.recoverable()` 判断备份是否比项目文件新；项目页顶部列出「可恢复项目」，一键**恢复**（用备份覆盖项目文件并打开）或**忽略**（删除备份） |
| 图像基础路径 | `project.params["image_base_path"]` 记录基础路径，项目页可查看 / 修改；来源目录整体搬走时仍能据此定位 |
| 缺失检测 | `expected_image_count()` 取「`.mprj` 图像归档条目」「数据集统计导入张数」「当前扫描结果」的最大值，`missing_count()` 与之求差 —— 这样整盘搬走也能报出「缺失 N 张」 |
| 一键重定位 | `relocate_images(新根目录)`：按文件名 + 尾部目录名匹配，**全部找到**才切换来源（与 DLT「目录结构需一致」一致），否则只报告数量；切换成功后用 `_remap_recorded_paths()` **递归改写**项目里记录的旧路径（`image_order` / `image_excludes` / 标记映射等），保证图库顺序与逐图元数据不丢 |
| 偏好设置 | `ConfigManager.PREFERENCES`（QSettings 持久化）+ `views/dialogs/preferences_dialog.py`：默认项目目录、最近项目数量、启动打开最近项目、CPU 线程数、滚轮反向缩放、十字准线及不透明度、标注区域不透明度、图像默认亮度 / 对比度、按住 `Shift` 显示像素值、自动保存间隔；改动后广播 `preferencesChanged`，由主窗口统一下发到画布 / 训练 / 项目管理 |
| 其他入口 | `F1` 快捷键总览、`Ctrl+,` 偏好设置、帮助菜单「打开日志目录」、拖放 `.mprj` / 图片 / 文件夹、`Alt+1..8` 切页 |

### 6.10 图库：自定义筛选规则 / 标签统计 / 显示增强

参照 Halcon DLT 的筛选与显示能力落地，界面只消费结构化结果：

| 能力 | 实现 |
|------|------|
| 自定义筛选规则 | `models/filter_rules.py`：字段 11 个（名称 / 路径 / 标注状态 / 备注 / 图像标记 / 标注类别 / 数据集拆分 / 标注数量 / 宽 / 高 / 通道数），关系按值类型区分（文本 7 种含正则、数值 6 种、列表 3 种、选择型 2 种）；规则是 **JSON 可序列化的树**（`logic` + `conditions`，支持嵌套子组），随项目保存在 `project.params["filter_rules"]` |
| 规则求值 | `FilterRules.match(context)` 逐条判定；**值为空或禁用的条件视为不约束**；`DatasetViewModel.filter_images(rules=...)` 把规则与原有筛选（标签 / 类别 / 标记 / 拆分 / 文本）叠加 —— 默认取项目里保存的规则，因此刷新后依然生效 |
| 按需读图 | 只有规则真正用到宽 / 高 / 通道数时才读图像头（PIL 不解码像素，按路径缓存）；用到标注数量 / 类别时才解析标签文件；备注读项目同级 `notes/` |
| 规则弹窗 | `views/dialogs/filter_rules_dialog.py`：「且 / 或」+ 条件行（字段 / 关系 / 值 + 候选值下拉 + 删除）+「添加条件」+ **预览命中数**（每次改动即时统计） |
| 标签统计 | `DatasetViewModel.label_statistics(paths, scope)` 汇总类别 / 拆分 / 标记的数量与占比 + 已标注覆盖；`views/dialogs/label_stats_dialog.py` 用三张表展示，可切「全部图像 / 当前选中图像」 |
| 显示增强 | `utils/image_ops.py::adjust_pixmap()` 用 256 级查找表调整亮度 / 对比度（numpy 向量化，仅 RGB 通道）；`ThumbnailDelegate` 按「图片 + 档位 + 参数」缓存结果，**只影响显示、不改文件**；类别名以半透明标签叠加在缩略图左上角，宽度按比例可调 |
| 类别名来源 | `DatasetViewModel.class_names_for()` / `gallery_decorations()["classnames"]`；分类任务的「来源根目录下的图片」不再被当成类别名（视为无标签） |

### 6.11 评估：误检细分 / GT 叠加 / 实例视图 / 多子集抽样

| 能力 | 实现 |
|------|------|
| 误检细分五类 | `evaluation_service.match_boxes()` 在匹配之外记录原因：**类别错**（与其它类别真实框 IoU 达标）、**无重叠**（附近没有真实框）、**定位不准**（同类真实框存在但 IoU 不足，下限 `PARTIAL_IOU = 0.10`）、**重复**（同一真实框已被更高置信度预测占用）、**多重错误**（与 ≥2 个真实框显著重叠）；返回 `matches / fp_details / fn_details`，漏检记录带同类最佳 IoU |
| 指标汇总 | `build_metrics()` 增加 `fp_summary`（五类 + 合计）与 `fn_total`；混淆矩阵口径不变（误检进最后一行、漏检进最后一列），逐类 FP / FN 按整列 / 整行统计 |
| GT 叠加图 | `evaluation_service.render_overlay()` 用 Pillow 把**真实框（绿实线）**与**预测框（橙虚线）**画在同一张图上（含中文标签与图例），落到 `data/eval/<stem>_gt.jpg`，右栏「GT 叠加」查看 |
| 排序 | `EvaluationConfig.eval_sort` + `_sort_eval_rows()`：顺序 / 置信度 ↓↑ / IoU ↑ / 错误优先；**排序在 ViewModel 内完成并同步当前选中行**，页面只需重读结果 |
| 实例级视图 | `EvaluateViewModel.build_instances()` 把每个真实框 / 预测框拆成一条记录（`kind = tp / fp / fn`、误检原因、IoU），`crop_instance()` 裁剪小图到 `data/eval/inst_*.jpg`；页面上「整图 / 实例」切换，点实例块联动到所属图像 |
| 多子集 + 抽样 | `EvaluationConfig.eval_subsets`（train / val / test 多选，勾选框）、`eval_random` + `eval_seed`；`_eval_plan()` 汇总多个子集的样本并打上 `subset` 标记，`_sample_samples()` 按上限截取或**用固定种子随机抽样**（可复现），结果记录带「图像集」供详情展示 |
| 详情面板 | 新增「图像集」一行；预览在「原图 / 预测图」之外增加「GT 叠加」（无叠加图时该按钮禁用）与「预处理」 |
| 异常后处理 | `anomaly_scores / anomaly_median / anomaly_best_threshold`（中位阈值与 F1 最优阈值）+ `set_anomaly_threshold / set_anomaly_tolerance`；左栏「异常检测」卡片内置**分数直方图**（`HistogramView`：按真实类别着色，叠加当前 / 中位 / 最优三条阈值线，**可点击拖动调阈值**）、分类阈值滑块、「中位阈值 / 最优阈值」按钮与分数容忍度滑块；**分数容忍度**等价于把判定门槛抬高为 `阈值 × (1 + 容忍度)`，重判后立即重算混淆矩阵与指标（分数本身不变，直方图不跳） |
| 预处理对比 | `EvaluationService.preprocess_preview()` 按训练口径做 letterbox 后与原始图并排（含中文标注与原图尺寸），右栏「预处理」查看；按「图片 + 输入尺寸」缓存，重复查看不重算 |
| 异常热图 | `AnomalibService.predict()` 若拿到 `anomaly_map` 会把原始分数图存到 `data/anomaly/<stem>.npy`（记录里为 `heat_map`）；`utils/image_ops.overlay_heatmap()` 用 JET 映射后按 alpha 叠加到原图，评估页右栏「热图」查看（无热力图时按钮禁用） |
| 最小缺陷尺寸 | `EvaluationService.abnormal_regions()` 用连通域统计**超过阈值的区域面积**（降序，像素）；`EvaluationConfig.anomaly_min_size` 设定后，只有最大区域仍小于该尺寸的高分图会被判回正常（噪声过滤）；**没有热力图时退化为按分数判定**，不静默改变结果 |

### 6.12 工程化：命令行 / 日志 / 报告样本图 / 导出精度

| 能力 | 实现 |
|------|------|
| 命令行参数 | `app.parse_args()`：`--project/-p`（启动后打开项目）、`--reset-preferences`（恢复默认偏好）、`--version`（打印 `APP_NAME APP_VERSION` 后退出，不启动界面）、`--log-level`；未知参数原样交给 Qt，`MainWindow` 之后的打开逻辑与「最近项目」共用一条路径 |
| 日志轮转可配 | `ConfigManager` 新增 `pref/log_backup_count`（默认 10 份）与 `pref/log_max_mb`（默认 5MB），`setup_logger(backup_count, max_bytes)` 据此创建 `RotatingFileHandler`；偏好设置弹窗可改（重启生效），帮助菜单可直接打开日志目录 |
| 恢复默认偏好 | `ConfigManager.reset_preferences()` 把 `PREFERENCES` 全部写回默认值（不动最近项目与主题），供 `--reset-preferences` 与界面调用 |
| 报告样本图 | `ModelReport` 新增「预测样本」段：按 **正确 / 误检 / 漏检** 分组，每类最多 3 张，HTML 版把图片缩放后**内联为 data URI**（报告是单文件、可直接分享或打印），Markdown 版列出说明与路径；样本由 `ExportViewModel._evaluation_samples()` 从评估结果挑选（优先用 GT 叠加图，其次预测可视化图） |
| 半精度导出 | `ExportConfig.half` + 导出服务参数白名单（ONNX / TorchScript 支持 half），界面「半精度（FP16）」勾选项，仅勾选时下传；「针对推断」与「针对 API」两个优化方向**互斥**（勾一个自动取消另一个） |

### 6.13 训练设置管理 / 参数预览 / 类别权重

| 能力 | 实现 |
|------|------|
| 训练设置（setup） | 命名保存多套训练参数，随项目保存在 `project.params["training_setups"]`（含 `active_setup_id`）：`TrainViewModel.setups / active_setup / create_setup / duplicate_setup / rename_setup / delete_setup / apply_setup`。切换时**先把当前配置存回原设置**（`_store_active()`）再载入目标设置，因此来回切换不会丢参数；快照用「`to_dict()` 去掉运行状态与历史」（`_RUNTIME_KEYS`），新增配置项自动纳入 |
| 训练记录归属 | 每条记录写入 `setup_id` / `setup_name`（哪套设置跑的）与 `archive_folder`（产物在项目里的归档目录）；产物按 `runs/<保存目录>-<时间戳>/` 归档，多次训练互不覆盖。单套设置下界面不显示「设置」列，避免噪音（见 §8.15） |
| 参数预览 | `utils/image_ops.build_param_preview()`：取训练集前若干张图，按训练口径 **letterbox** 到预览边长并叠加**增强示意**（水平 / 垂直翻转、旋转、缩放、色彩抖动），拼成一张带说明的拼图；`augment_preview_image()` 返回生效的增强项，说明文字明确标注 mosaic / mixup 未体现。训练页「预览参数」按钮生成后用通用 `ImagePreviewDialog` 查看 |
| 类别权重 | `TrainViewModel.train_class_counts()` 统计训练集类别分布（分类按类别目录图片数、检测 / 分割按标签**框数**、异常按 normal / abnormal），`suggest_class_weights()` 给出「逆频次 + 均值归一化为 1」的权重，「平衡 (BALANCE)」写入 `TrainingConfig.class_weights`，「重置」清空。**说明**：Ultralytics 当前不消费该权重，此项用于判断数据是否均衡、是否需要补样本或改用单类训练 |
| 通用预览弹窗 | `views/dialogs/preview_dialog.py::ImagePreviewDialog`：按窗口缩放显示图片 + 说明，供参数预览（后续热图 / Grad-CAM 也复用） |

### 6.14 训练暂停 / 继续（轮边界控制文件）

| 环节 | 实现 |
|------|------|
| 控制通道 | 父进程与训练子进程约定一个**控制文件** `<project_dir>/train.pause`（`TrainService.pause_file()`）；路径通过 `--pause-file` 传给子进程，因此不需要额外 IPC |
| 子进程 | `train_worker.wait_if_paused()` 在 `on_train_epoch_end`（**轮边界**）检查该文件：存在则上报 `{"type": "status", "status": "paused"}` 并原地等待（0.5s 轮询），文件被删除后上报 `running` 并继续训练。因此在轮边界暂停**不丢进度**，也不会占用下一次训练的显存峰值 |
| 父进程 | `TrainService.pause / resume / is_paused`；`stop()` 会先移除标记，避免下次训练一启动就被挂起 |
| ViewModel | `TrainViewModel.pause / resume / is_paused`，并把子进程的 `status` 事件转成训练状态（`paused` / `running`）与日志；`status` 也参与界面按钮状态 |
| 界面 | 结果页按钮行新增「暂停 / 继续」（按状态自动切换文案），空闲时状态显示「已暂停（轮边界）」 |
| 追加轮数继续 | `TrainViewModel.continue_training(extra_epochs)`：取最近一次训练记录的 `results.csv` 定位 `<run>/weights/last.pt`，把总轮数改成「已完成 + 追加」并置 `resume=True` 后重新启动，因此是同一次运行的断点续训；缺少 `last.pt` 时会明确提示而不是静默失败 |
| 异常检测 | Anomalib 训练走独立子进程且无轮回调，暂不支持暂停（界面按钮在非 YOLO 任务下同样可用，但只在 YOLO 训练时生效） |

### 6.15 OOD（分布外）检测

| 环节 | 实现 |
|------|------|
| 思路 | 用训练好的模型提取特征（分类头**前一层**的输出）→ 统计已知样本的均值与标准差 → 新样本按「各维独立」的简化马氏距离判定，超过阈值即为分布外 |
| 统计量 | `services/ood_service.py::OodStats`：`from_features()` 拟合（阈值 = 样本距离的 **95 分位**，可调）、`distance()` 计算距离（维度不符返回 -1 表示无法判定）、`is_ood()` 判定、`to_dict / from_dict / save / load` 支持保存为 JSON 供后续直接加载 |
| 特征层 | `OodService`：`features()` 单图特征、`fit()` 用目录拟合、`predict()` 批量判定；`supported()` 先检查权重是否为**分类模型**（检测 / 分割没有可直接取用的分类头，会给出明确提示而不静默失败） |
| 用途 | 拦截「模型没见过的图」：例如把明显不属于训练集分布的样本挡在外面，避免模型给出不可信的高置信输出 |
| 界面入口 | 评估页左栏「OOD 检测」按钮 → `views/dialogs/ood_dialog.py`：填模型权重 + 已知样本目录 →「拟合分布」（显示样本数与阈值）→ 选待测图片 / 目录 →「判定」列出每张图的距离与「分布内 / 分布外」 |

### 6.16 Grad-CAM（分类热力图解释）

| 环节 | 实现 |
|------|------|
| 算法 | `services/gradcam_service.py`：在骨干**最后一个卷积层**挂前向 / 反向钩子，取该层激活与目标类别（默认取预测类别）的梯度，用梯度的通道均值加权激活 → ReLU → 归一化成 0~1 的热力图 |
| 叠加 | 复用 `utils/image_ops.overlay_heatmap()`（JET + alpha 混合），评估页右栏新增**「Grad-CAM」预览** |
| 适用范围 | **仅分类模型**（`supported()` 校验有 `classifier` 头）；检测 / 分割不支持，按钮默认禁用、不支持时静默返回空而不报错 |

### 6.17 项目文件夹与删除（参照 Halcon DLT）

| 环节 | 实现 |
|------|------|
| 项目文件夹 | `<项目文件（不含扩展名）>/`，与 `.mprj` 同级；**新建项目时创建**，旧项目在首次产生产物时按需创建 |
| 产物落位 | 数据拆分 `<项目文件夹>/<拆分名>/`、数据增强 `<项目文件夹>/augment/`、归档释放 `<项目文件夹>/files/`、训练 `<项目文件夹>/runs/`、导出 `<项目文件夹>/export/` |
| 删除清单 | `ProjectService.artifacts()` 列出：项目文件、项目文件夹、自动备份 `.bak`、文件夹内的训练产物（`.pt` / `.ckpt` / `.torchscript`），以及**旧版**留在项目文件同级的拆分 / 增强目录 |
| 删除确认 | `views/dialogs/delete_project_dialog.py`：标题＝动作，正文列出将永久删除的路径与训练产物数量，底部可「打开项目文件夹」先取走模型 |
| 删除执行 | `ProjectService.delete_project()` 递归删除文件夹（只读文件先清属性）与文件；返回 `removed` / `failed`，界面按结果提示 |
| 安全边界 | 旧产物目录只有在**项目文件所在目录之内**时才删除，避免误删用户放在别处的数据 |
| 兼容 | 旧项目的拆分目录记录在 `Split.output_dir`，打开与训练不受影响，删除时会被一并列出并清理 |

### 6.18 类别配色规则（按名称语义初始化）

新建类别时按**名称语义 + 类别类型**初始化颜色，避免「不良用绿色」这类误标。
实现见 `services/category_service.py`：`CategoryService.color_for(name, kind, used)`。

**只作用于新建类别**：已有类别、用户手工改过的颜色不会被覆盖。适用入口：
图像导入自动建类、类别管理「新增」、`classes.txt` / `data.yaml` 导入、数据集同步。

| 配色族 | 用色 | 语义 |
|--------|------|------|
| 良品族 | 绿 `#0F7B0F` → 蓝 `#005FB8` → 青 `#00838F` → 橄榄 `#4D6A00` → 深青绿 `#167A5C` → 靛蓝 `#3A5A8C` | 良品 / 合格 / 正常等普通标识色 |
| 异常族·重 | 红 `#C42B1C` → `#8A1F14` → `#D13438` | 影响功能或整件判废（断裂、短路、缺件、报废…） |
| 异常族·中 | 橙 `#9D5D00` → `#B86B00` → `#C05600` | 明确不良表述与主要缺陷（异常、不合格、划痕、裂纹…） |
| 异常族·轻 | 黄 `#C29300` → `#A88400` → `#8A6D00` | 外观类轻微瑕疵（脏污、色差、毛刺、麻点…） |
| 中性 | 灰 `#5C5C5C` / 紫 `#7A3E9D` / 品红 `#B4009E` / 紫灰 `#8764B8` / 棕 `#8A5A00` / 石板蓝 `#4B4B6E` | 名称无语义可判（`class_1`、`0`、`part_a`…） |

中性板刻意不含绿、红、橙、黄——无语义的类名不应被读成「良品」或「异常」。
三族色值互不重叠；同族内按「首选色 → 同档备用色 → 全异常族」逐级错开，
已占用色不再重复使用。

**判定顺序**（自上而下，首个命中即生效，顺序不可调换）：

| 序 | 规则 | 例 |
|----|------|-----|
| 1 | 类别类型为「异常」（异常检测项目） | `kind="abnormal"` → 异常族，严重度按名称细化 |
| 2 | 类别类型为「良好」 | `kind="normal"` → 良品族 |
| 3 | 否定式良品：`无 / 非 / 未` + 不良词；`NO / NON` + 不良词；不良词 + `FREE` | 无缺陷、未损伤、NO_DEFECT、NONDEFECT、DEFECT FREE |
| 4 | 判定级不良：整件判不良的表述 | 不合格、不良、异常、NG、NOTOK、DEFECT、ABNORMAL |
| 5 | 明确良品：良品 / 合格 / 正常 等 | 良品、合格品、NORMAL、OK、PASS |
| 6 | 具体缺陷名，按严重度取档 | 断裂（重）、划痕（中）、脏污（轻） |
| 7 | 以上都不命中 → 中性板 | `class_1`、`0` |

第 3 步必须在第 4 / 5 步之前：否则「无缺陷」会被「缺陷」判成异常、「不良」会被「良」判成良品。
第 5 步必须在第 6 步之前：否则「合格品」会被「缺口」类词干扰。

**匹配方式**：中文按子串（分隔符会拆散中文词，故先剥离非汉字字符）；
英文按词元，切分边界为非字母数字、驼峰与数字，并统一大写，
使 `NORMAL` 不会被 `NO` 误判、`defect1` 仍能命中 `DEFECT`、`SCRATCHES` 命中 `SCRATCH`。
判定级不良与三档缺陷词表见 `category_service.py` 顶部常量，可按现场术语直接增补。

> 类别类型（`ClassDef.kind`）由异常检测项目在导入弹窗中逐个指定，优先级高于名称语义；
> 未指定时才按名称判定。用户改色入口仍是类别管理页与类别编辑弹窗，规则只在新建时生效一次。

### 6.19 异常检测的数据拆分（按类别分配）

异常检测**只用正常样本训练**（见 `tasks.py` 的「仅用正常样本」），因此拆分不能只按
总比例分配——那样会把 NG 图按比例塞进训练集。异常检测项目的拆分改为按类别分配，
界面参照 Halcon DLT 的「创建拆分」：良好图像 / 异常图像各自给出训练、验证、测试的数量与百分比。

| 环节 | 约定 |
|------|------|
| 类别来源 | 图片类别（导入时按文件夹指定）→ 项目类别表的 `ClassDef.kind`（`normal` / `abnormal`） |
| 类别类型未指定 | 按「良好」处理（导入弹窗的默认值就是良好） |
| 无类别的图片 | 不参与分配（与 DLT 的「已标注 / 全部」一致，界面上给出该计数） |
| 训练集 | **只放良好图**：异常图的训练数量恒为 0，界面禁用该格（用控件可用性表达约束） |
| 验证 / 测试 | 良好与异常都可放；异常图默认按项目比例落到验证 / 测试 |
| 数量语义 | 每个类别的「训练 + 验证 + 测试」恒等于该类图片数：改一格时其余行按比例吸收差额 |
| 与图片数不一致 | 数量按**权重**使用（最大余数法归一化），图片增删后既不丢图，也不会把异常图挤进训练集 |
| 落盘 | **Anomalib Folder 结构**（`LAYOUT_ANOMALY`）：`normal/` 训练良好图、`normal_test/` 验证 + 测试良好图、`abnormal/` 验证 + 测试异常图；产物目录本身即数据集，不生成 data.yaml |
| 与训练打通 | 执行 / 切换拆分后，训练配置的「异常检测数据目录」自动指向当前拆分产物；训练只用 `normal/` 拟合记忆库（用户自选的外部目录不被覆盖） |
| 与评估打通 | 异常评估取 `normal_test/` + `abnormal/`（跳过 `normal/` 训练图，避免把训练集算进指标）；用户自选的平铺目录（只有 `normal/` + `abnormal/`）原样使用 |
| 持久化 | 每个拆分各自保存 `Split.anomaly_counts`，比例字段 `train/val/test` 由分配结果反算 |

实现：`services/dataset_service.py::_allocate_by_kind()`（算法，落盘见
`split_dataset()` 的 `anomaly_folder` 分支）与 `views/split_tab.py`（分配表）；
训练目录同步见 `dataset_vm._sync_anomaly_root()`，评估数据源见
`evaluate_vm.anomaly_eval_sources()`。预览、执行拆分、图库子集角标共用同一份分配，
因此「看到的」与「落盘的」一致。

## 七、风险与应对

| 风险 | 应对措施 |
|------|---------|
| GPU环境配置复杂 | 提供详细的环境配置文档，支持CPU模式降级运行 |
| 训练过程耗时长 | 实现训练状态保存与恢复（checkpoint），支持断点续训 |
| 大数据集加载慢 | 使用懒加载和分页加载，优化内存管理 |
| 跨平台兼容性 | 尽早进行跨平台测试，使用条件编译处理平台差异 |
| 标注器开发工作量大 | 先实现「矩形框 + 多边形」最小闭环，分类标签复用同一画布；并提供标注格式互通兜底 |
| SAM / Anomalib 依赖较重 | 按阶段安装、服务层懒加载，未安装时对应入口降级提示 |
| 智能标注误检率高 | 预标注结果一律作为「候选」，必须经人工确认后才写入标签 |
| 开源协议传染风险 | 禁止链接 GPL / AGPL 的外部标注工具（X-AnyLabeling、Label Studio），仅通过标注文件交换 |
| Ultralytics AGPL-3.0 | 本项目定位内部使用 / 自身开源，AGPL 无碍；若未来闭源分发须购买 Enterprise 授权 |

## 八、多模型共用平台改造方案

> 目标：让 YOLO 家族（检测 / 旋转框 / 分割 / 分类）、异常检测（Anomalib）与后续 OCR（PaddleOCR 等）、U-Net / 语义分割**共用同一套项目、数据、标注、训练、评估、导出流程**。
> 本章是改造规格（不改现有行为的部分先行重构），落地按 §8.7 分期执行。界面改动一律遵守 §4.3 文案规范。

### 8.1 现状定位

| 项 | 现状 |
|----|------|
| 项目类型 | `PROJECT_TYPES`（`constants.py:87-158`）已定义 7 类，其中 `semantic` / `ocr` 为 `supported: False` 占位 |
| 后端 | 只有两个，且**并列硬编码**：YOLO 走 `train_worker`，异常检测走 `anomaly_worker`，路由是 `if config.task_type == "anomaly"`（`train_service.py:56-65, 142-147`） |
| 已有资产 | 子进程 + JSON 事件协议（`train_worker.py:5-12`）、多套拆分、多套训练设置（§6.13）、后端无关的指标计算（`iou_xyxy` / `match_boxes` / `build_matrix` / `build_metrics`，`evaluation_service.py:41-423`） |
| 结构性问题 | ① 无后端抽象层，新增后端改动散落 20+ 处；② 结果 schema 两套（`boxes/probs` 与 `score/label`）互不兼容；③ 任务知识（指标 / layout / 模型候选 / 结果视图）散落在 6 个文件里，且 `_TRAIN_TASKS` 在 `train_vm.py:77` 与 `evaluate_vm.py:26` 重复定义 |

### 8.2 三样地基

#### 8.2.1 任务能力注册表（TaskSpec）

把散落的任务知识收敛为一张表，`PROJECT_TYPES` 升格为其数据源；`TASK_TYPES` / `TASK_MODEL_SUFFIX` / `PROJECT_ANNOTATION` 并入。

| 字段 | 含义 | 现状来源 |
|------|------|---------|
| `key` / `label` | 任务标识与显示名 | `constants.py:87-158` |
| `backend_candidates` | 可选后端列表（如 `["yolo"]`、`["anomalib"]`，分割可 `["yolo","unet"]`） | 现在由 `train_tab.py:921-939` 的 if/else 决定 |
| `annotation` | 标注模式：`none / box / obb / polygon / text` | `constants.py:160-163`（缺 `text`） |
| `dataset_layout` | 数据集落盘结构：`detect / classify / mask / ocr_det_rec / anomaly_folder` | `dataset_vm.py:660, 2067-2072`（只有二元） |
| `metrics` | 曲线 / 主指标候选键名 | `train_vm.py:59-77` `_METRIC_SPECS`（表驱动，直接迁移） |
| `eval_views` | 评估页结果卡片标识 | `evaluate_tab.py:931-959`（硬编码 `== "anomaly"`） |
| `export_formats` | 可用导出格式集合 | `constants.py:187-191`（全局固定） |
| `pages` | 本任务需要的导航页集合 | 无（`main_window.py:344-402` 固定 8 页） |
| `quick_fit` | 是否为「快速拟合」语义（异常检测记忆库构建） | 无 |

同时修复：`project_type()` 的兜底是位置索引 `PROJECT_TYPES[2]`（`constants.py:171`），改为按 key 查 `"detect"`。

#### 8.2.2 后端适配层（BackendAdapter）

```
src/services/backends/
├── __init__.py     # 注册表：register / get / all_keys / detect_for(task)
├── base.py         # BackendAdapter 接口
├── yolo.py         # 现 train_worker + yolo_service + inference_service 迁入
├── anomalib.py     # 现 anomalib_service + anomaly_worker 迁入
└── ocr.py          # 未来
```

| 方法 | 职责 | 现状落点 |
|------|------|---------|
| `is_available() / check_env()` | 依赖探测与环境报告 | 现在各自为政（`ultralytics` 出现在 6 个服务里） |
| `validate(config)` | 启动前数据校验 | `train_service.py:56-65` |
| `build_args(config)` | 构造子进程命令行 | `train_service.py:142-147` + `_anomaly_args / _yolo_args` |
| `parse_event(line)` | 解析子进程 JSON 事件 | 事件协议 `train_worker.py:5-12`，由 `train_vm._on_event()` 无条件消费 |
| `artifacts(summary)` | 产物清单 | `train_vm.py:1060-1078`（现在只认 `best.pt/last.pt/results.csv`） |
| `predict(...) / evaluate(...) / export(...)` | 推理、评估、导出 | `evaluation_service.evaluate`（`:449-451` 绑 `YOLO`）、`export_service._get_yolo`（`:28-36`） |

事件协议（`phase / iteration / epoch / metrics / done / error`）提升为正式契约：新后端只要遵守它即可复用整套训练页 UI。暂停协议（`wait_if_paused`，`train_worker.py:46-65`）同样上提为可选契约，未实现的后端在界面禁用按钮。

#### 8.2.3 统一结果契约

| 契约 | 内容 | 取代 |
|------|------|------|
| `Prediction` | `image / task / backend / boxes(mask) / score / cls / text` | YOLO 的 `boxes·probs·plot()`（`inference_service.py:53-107`）与 Anomalib 的 `score·label`（`anomalib_service.py:116-131`）两套 |
| `Artifacts` | 主模型文件 + 附属文件清单（`kind / path / required`） | 各页自行拼路径 |
| `EnvReport` | 后端可用性、缺失依赖、解释器路径 | 分散的 `is_available` |

评估、报告、导出只依赖契约；`evaluate_vm._run_anomaly_evaluation`（`:798-849`）这类平行实现随之取消。

### 8.3 服务层改造清单

| 环节 | 现有实现 | 改造项 |
|------|---------|--------|
| 项目 | `Project.model_type` 单字段兼任「任务」与「后端」（`project.py:56`） | 拆 `task` + `backend`；`.mprj` 版本号升级并做迁移（§8.9） |
| 数据集 | `split_dataset` 仅 `detect / segment / classify` 三种 layout（`dataset_service.py:523-605`）、`write_data_yaml` 仅 YOLO（`:764-789`） | layout 由 `dataset_layout` 映射表决定；新增 `mask`（像素掩码 PNG）、`ocr_det_rec`（检测框 + 转写）、`anomaly_folder`（normal/abnormal）三种落盘器；统一 `normal/abnormal` 目录约定（现在 `constants.py:173-175` 是**未被引用的死常量**，实际默认值在 `anomalib_service.py:32-33`，评估侧又有一套 `ng/defect` 别名，`evaluate_vm.py:819-820`） |
| 训练 | 分支路由 + 单一扁平 `TrainingConfig`（`training.py:16-64`，YOLO 与 anomaly 字段混居） | `TrainingConfig` 拆「通用参数 + `backend_params` 子配置」；`TrainService` 改为查表取 adapter；训练设置（setup）扩展为可归档产物与指标（现 `history` 是项目级平铺，`train_vm.py:1033-1050`） |
| 评估 | 分派靠 `task == "anomaly" or 后缀 == ".ckpt"`（`evaluate_vm.py:667-671`）；指标字段硬编码（`report_service.py:206-223`） | `Evaluator` 按任务范式注册；报告指标改为按任务取字段；OCR 需 `CER / WER / 精确匹配` |
| 导出 | 仅 YOLO 三格式（`export_service.py:13-36`）；`.ckpt` 无路径（传入会走 `YOLO(ckpt)`） | 按后端分派；异常检测「模型包」（`backbone.onnx` + 记忆库 + `config.json`）；OCR 子模型（det / rec / cls）+ 字典文件；**导出后精度回归测试**（当前完全缺失） |
| 解释器 | `QProcess.start(sys.executable, args)`（`train_service.py:86`） | 解释器改由后端配置提供（§8.6） |

### 8.4 界面层改造清单

#### 8.4.1 通用机制：页面能力门控

| 项 | 现状 | 改造 |
|----|------|------|
| 导航页 | 固定 8 页硬编码（`main_window.py:344-402`），`supported` 字段没有被任何导航逻辑消费 | 按 `TaskSpec.pages` 显示 / 隐藏页面；`_init_shortcuts`（`main_window.py:407-421`）的 `Alt+1..8` 改为**按可见页顺序重建**，否则隐藏页会导致编号错位 |
| 页面同步 | 只有 `annotate_tab.apply_project_type()` 与 `train_tab.sync_from_config()` 两处零散通知（`main_window.py:334-339`） | 主窗口在任务变化时统一调用**页面可选的 `apply_task(spec)`**（未实现则跳过）；页面自身差异用控件可用性表达 |
| 表达方式 | — | 遵守 §4.3：用置灰 / 隐藏 / 页签命名表达能力约束，不加说明句；需要解释的放 tooltip（≤ 20 字） |

#### 8.4.2 逐页改动

| 页面 | 改动内容 | 落点 |
|------|---------|------|
| 新建项目 | 放开 OCR / 语义分割卡片；`supported` 语义由「规划中」改为「后端未就绪」；标注方式文案补 `text`；详情面板的「训练模型：YOLO11×」硬编码改为按后端显示；创建时记录 `backend` | `new_project_dialog.py:51,64-66,88-92,220-246,342-348`；`project_service.py:59-66` |
| 项目管理 | 项目卡片显示「任务 · 后端」；「· 规划中」按后端就绪度显示 | `project_tab.py:543-550` |
| 图库 | 异常项目：normal / abnormal 标记、统计与筛选（现在异常项目落 `detect` layout）；OCR 项目：「已转写 / 未转写」角标 + 转写文本查看；筛选项增加文本相关字段 | `dataset_vm.py:660,2067-2072`；`filter_rules.py`；`gallery_tab.py` / `data_widgets.py` |
| 图像标注 | 文本标注模式：工具条按任务重建（OCR 项目为「浏览 + 文本」）、画布拖出文本框、编辑区「文本」行录入转写、对象列表显示转写；转写走旁路文件存储（YOLO txt 保持标准）；词典 / 字符集入口待训练接入时一并补 | `models/annotation.py`；`annotate_tab.py`；`annotation_canvas.py`；`annotation_service.py` |
| 标注检查 | 显示转写文本；框与文本一致性检查；异常项目按分数 / 热图展示 | `review_tab.py:175-177,301-310` |
| 数据拆分 | layout 由任务映射表决定（已实现 classify / detect / ocr_det_rec / mask 四种）；异常检测「仅 good 参与比例、异常图按绝对数量入验证集」待接入 | `split_tab.py`；`dataset_vm.split_layout()`；`dataset_service.split_dataset()` |
| 模型训练 | 模型下拉改查表（`BackendRegistry.candidates(task)`）；参数区改「通用 + 后端专属」两段（`QStackedWidget`），现有 `anomaly_row` 成为 anomalib 段首个实例；OCR 需 det / rec / cls 子模型 + 词典 + 字符集；异常检测按钮与状态用「快速拟合」语义；训练设置（setup）登记产物与指标 | `train_tab.py:488-501,845-855,906-939,995-999` |
| 模型评估 | 结果卡片改为注册表（现 `_fill_anomaly` 硬编码 `task == "anomaly"`）；指标按任务范式分组、AUROC 不与 mAP 同表；OCR 卡（识别文本表 + CER/WER + 逐图对错）；权重过滤器由后端声明（现为 `"*.pt *.ckpt"`）；OOD / Grad-CAM 按钮按后端能力置灰（`gradcam_available()` 已是先例） | `evaluate_tab.py:189-192,232-286,931-959,1256-1265` |
| 模型导出 | 格式下拉改由**后端提供**（`BackendAdapter.export_options()`：YOLO 三格式 / 异常检测「模型包」），选中不同权重时自动重建；导出后自动回归并在「导出记录」增加回归列；OCR 子模型与字典同机制接入 | `export_tab.py`；`export_vm.py`；`backends/*.py` |
| 偏好设置 | 新增「后端解释器」列表（每后端一个路径 + 检测按钮）与依赖状态；新增偏好键写入 `values()` | `preferences_dialog.py:55-136,184-201` |

#### 8.4.3 新增文件（界面层）

| 文件 | 用途 |
|------|------|
| `views/dialogs/backend_env_dialog.py` | 后端环境与解释器配置、依赖状态 |
| `views/dialogs/text_dict_dialog.py` | 词典 / 字符集管理（OCR） |
| `views/widgets/text_annotation.py` | 文本框标注工具（也可并入 `annotation_canvas.py`，按实现成本二选一） |

#### 8.4.4 界面不做的事

| 项 | 理由 |
|----|------|
| 不为 OCR 单开导航页 | 与异常检测「不单独设导航页」的既有约定一致（§4.1 注释） |
| 不在页面上写后端差异说明 | 遵守 §4.3；差异用控件可用性与 tooltip 表达 |
| 不新增装饰性元素（含 LOGO 进入窗体内部） | 保持界面只承载操作 |

### 8.5 数据结构与接口定义（落地时的字段基线）

| 对象 | 字段 |
|------|------|
| `TaskSpec` | `key / label / backend_candidates / annotation / dataset_layout / metrics / eval_views / export_formats / pages / quick_fit` |
| `BackendAdapter` | `key / label / is_available() / check_env() -> EnvReport / validate(config) -> str \| None / build_args(config) -> list[str] / parse_event(line) -> dict \| None / artifacts(summary) -> list[Artifact] / predict(...) / evaluate(...) / export(...)` |
| `Project` | 新增 `backend: str`（`"yolo" / "anomalib" / "ocr"`），`model_type` 更名语义为 `task` |
| `TrainingConfig` | 通用字段保留；后端专属字段移入 `backend_params: dict`（yaml / ckpt / 词典路径等） |
| `Artifacts` | `kind / files[{path, required}] / meta`（异常：`backbone.onnx + memory_bank + config.json`；OCR：`det / rec / cls + dict`） |

### 8.6 依赖与环境隔离

| 项 | 方案 |
|----|------|
| 依赖清单 | `requirements.txt` 保留核心（PySide6 / torch / opencv）；按后端拆 `requirements-backend-yolo.txt`、`requirements-backend-anomalib.txt`、`requirements-backend-ocr.txt` |
| 解释器 | `ConfigManager` 新增 `pref/backend_python/<backend>`；`TrainService` 用它替换 `sys.executable`（`train_service.py:86`），未配置则回退当前解释器 |
| 探测 | `check_env()` 在启动与新建项目时调用，结果驱动 §8.4.2 的置灰状态；缺失依赖只提示缺什么，不写长篇说明 |

### 8.7 分期实施计划

| 期 | 内容 | 验收 |
|----|------|------|
| 第 1 期（地基）✅ 已完成 | `TaskSpec` 注册表 + `BackendAdapter` 接口 + 注册表路由；YOLO 与 Anomalib **行为不变**地走同一接口；`backend` 字段与 `.mprj` 迁移；页面能力门控骨架 | 现有检测 / 分割 / 分类 / 异常四条链路回归通过，快捷键与页面显示无变化（实现见 §8.10） |
| 第 2 期（补短板）✅ 已完成 | 统一结果契约；异常检测导出「模型包」；导出后精度回归；评估结果卡片注册表化 | 异常模型可从导出页产出可加载的模型包；导出后自动回归并给出结论（实现见 §8.11） |
| 第 3 期 A（OCR 数据链路）✅ 已完成 | 文本标注（文本框 + 转写）→ det / rec 数据产物 → CER / WER 指标 → OCR 任务与后端门控 | OCR 项目可标注并导出 det / rec 数据集；训练入口被明确拦截而不是回退 YOLO（实现见 §8.12） |
| 第 3 期 B（OCR 模型链路，需 Paddle 环境） | OCR worker（遵守事件协议）→ OCR 评估卡 → 子模型导出（det / rec + 字典） | 需先具备 `paddle` + `paddleocr` 运行时（当前 Python 3.14 无可用轮子，见 §8.12） |
| 第 4 期 A（语义分割 / U-Net）✅ 已完成 | 掩码数据集布局 + 轻量 U-Net 训练闭环（worker 遵守事件协议、支持轮边界暂停）+ mIoU / Dice 指标 | 语义分割项目可标注 → 生成掩码数据集 → 训练出可用的 `.pt`（实现见 §8.13） |
| 第 4 期 B（语义分割评估与导出）✅ 已完成 | 语义分割评估（像素级 mIoU / Dice / 像素准确率 + 逐类 IoU + 叠加对比图）、TorchScript 导出（含输入尺寸元信息）、导出回归按掩码一致率判定 | 语义分割全链路打通（实现见 §8.14） |
| 第 4 期 C（多路线归档）✅ 已完成 | 训练记录写入 setup 归属与归档目录；产物按 `<保存目录>-<时间戳>` 独立归档；训练记录 / 对比选择 / 指标对比三处表格显示「设置」列 | 同项目多套训练设置可横向对比，且各次训练产物互不覆盖（实现见 §8.15） |

### 8.8 与 QA.md 建议的取舍

| QA.md 建议 | 本项目取舍 | 理由 |
|-----------|-----------|------|
| 以 COCO 为中间格式真源 | 不引入。内部 `Annotation`（归一化点 + kind，`annotation.py`）已是等价中间表示；仅需补 `text` 与按后端的转换 / 导出层；COCO 可留作可选导入导出桥 | 保持 `.mprj` 单文件定位，避免双真源 |
| 集成 MLflow / W&B | 不做 | 单文件项目 + 训练记录 / 训练设置已构成 Tracking；引入外部服务破坏离线定位 |
| Docker / 多 Conda 环境 | 折中：按后端拆分依赖清单 + 可配解释器 + 环境探测 | 复用现有子进程架构，改动小 |
| 同项目内并存多种模型路线 | 第 4 期再做 | 第 1~3 期先保证「一项目一任务、多后端可换」 |
| 导出后精度回归 | 采纳，列入第 2 期 | 当前完全缺失，是最容易埋雷的一环 |

### 8.9 迁移与兼容

| 项 | 处理 |
|----|------|
| `.mprj` 版本 | `PROJECT_VERSION` 1 → 2；旧项目读入时 `model_type == "anomaly"` → `backend="anomalib"`，其余 `backend="yolo"`；写回时按新版本 |
| 常量清理 | 修复 `project_type()` 位置兜底（`constants.py:171`）；启用或删除死常量 `ANOMALY_NORMAL_DIR / ABNORMAL_DIR`（`:173-175`）；统一 `abnormal` 目录别名（`evaluate_vm.py:819-820`） |
| 重复定义 | `_TRAIN_TASKS`（`train_vm.py:77` / `evaluate_vm.py:26`）合并到 `TaskSpec`，消除两份 |
| 兼容底线 | 旧项目打开后行为不变；新增字段一律给默认值，缺失时按旧逻辑回退 |

### 8.10 实施记录（第 1 期）

**新增文件**

| 文件 | 作用 |
|------|------|
| `src/utils/tasks.py` | 任务能力注册表（TaskSpec）：任务、标注方式、数据布局、可用后端、指标、评估视图、导出格式、所需导航页、快速拟合 |
| `src/services/backends/base.py` | 适配器接口 + `Problem`（message / hint）/ `EnvReport` / `Artifact` + 暂停控制文件 |
| `src/services/backends/yolo.py` | Ultralytics YOLO 后端（命令行参数自 `TrainService._yolo_args` 迁入） |
| `src/services/backends/anomalib.py` | Anomalib 后端（命令行参数自 `TrainService._anomaly_args` 迁入） |
| `src/services/backends/__init__.py` | 后端注册表：`register / get / for_task / resolve`，默认注册两个后端 |

**改动文件**

| 文件 | 改动 |
|------|------|
| `utils/constants.py` | `PROJECT_TYPES` / `PROJECT_ANNOTATION` / `TASK_MODEL_SUFFIX` 改为由注册表派生；删除未被引用的 `TASK_TYPES`；`project_type()` 兜底由位置索引改为按 key；`PROJECT_VERSION` 1 → 2 |
| `services/train_service.py` | 启动校验与命令行参数改为「按任务查后端」；删除 `_yolo_args` / `_anomaly_args`；`pause_file` 转调 `backends.base` |
| `viewmodels/train_vm.py` | 删除 `_METRIC_SPECS` / `_TRAIN_TASKS` / `_task_of`（改用注册表）；启动校验走适配器；产物归档走 `artifacts()` |
| `viewmodels/evaluate_vm.py` | 删除重复定义的 `_TRAIN_TASKS`，改用注册表 |
| `viewmodels/dataset_vm.py` | 数据布局改由注册表判定（`split_layout()` 成为单一来源） |
| `views/train_tab.py` | 模型下拉候选改由后端提供（删除 anomaly 分支与后缀拼接） |
| `models/project.py` | 新增 `backend` 字段；旧项目按项目类型自动补齐 |
| `services/project_format.py` | 版本守卫改为「支持 v1 / v2」，密钥按写入版本派生，旧项目可读 |
| `services/anomalib_service.py` | `normal` / `abnormal` 目录约定改用 constants 常量（消除重复定义） |
| `views/shortcuts.py` | 快捷键支持 `tag` 与 `remove_tag`（按标签整组重建） |
| `views/main_window.py` | 导航页登记页面 key；新增 `_apply_task_pages` / `_rebuild_page_shortcuts`；`Alt+1..N` 按可见页重建 |

**行为保持**

| 项 | 说明 |
|----|------|
| 校验文案 | 服务层提示语与界面引导语原样保留（`Problem.message` / `Problem.hint`） |
| 模型候选 | 检测族仍为 `YOLO_MODEL_VARIANTS + 任务后缀`，异常仍为 Anomalib 五个模型 |
| 页面与快捷键 | 已实现任务均使用全部 8 页，`Alt+1..8` 编号不变 |
| 旧项目 | v1 容器可正常打开，读入即补齐 `backend`，保存后升级为 v2 |

**已知差异（有意为之）**

- 异常检测的产物归档此前会把同一个 `.ckpt` 登记两次（`best` 与 `last` 同文件），现按路径去重，归档提示数量由 2 变 1。
- 异常检测训练前的校验从「目录非空」变为「目录须存在」（与 YOLO 一致，提示语不变）。

**验证**：offscreen 自检 39 项全部通过（任务表 / 后端路由 / 命令行参数 / 项目与容器往返 / v1 兼容 / 导航页门控 / `Alt+N` 重排）。

### 8.11 实施记录（第 2 期）

**新增文件**

| 文件 | 作用 |
|------|------|
| `models/prediction.py` | 预测结果契约 `Prediction` / `Box`：检测框、分类 top1、异常分数与判定、热图、OCR 文本；`positive` / `confidence` 统一口径 |
| `services/regression_service.py` | 导出后精度回归：源模型与导出产物逐图对比（分类看 top1，检测看「类别 + IoU」匹配），模型包做清单 / SHA256 / 记忆库校验 |

**改动文件**

| 文件 | 改动 |
|------|------|
| `services/backends/base.py` | 新增 `ExportResult`（单文件或模型包目录 + 文件清单 + 降级说明，`size_mb()` 按目录累加）；接口新增 `predict()` / `export_options()` / `export()` |
| `services/backends/yolo.py` | `predict()`（统一契约）、`export_options()`（三格式）、`export()`（委托 `ExportService`） |
| `services/backends/anomalib.py` | `export_options()`（模型包）、`export()`（委托 `AnomalibService.export_package`）；`predict()` 待部署端运行时接入（已注明原因） |
| `services/backends/__init__.py` | 新增 `for_weights(weights, task)`：`.ckpt` 一律走 Anomalib |
| `services/anomalib_service.py` | 新增 `export_package()`：`model.ckpt` + `memory_bank.npz`（记忆库 / 均值协方差）+（可选）`backbone.onnx` + `config.json`（逐文件 SHA256 与降级原因） |
| `models/training.py` | `ExportConfig` 新增 `anomaly_model`（模型包需要模型名） |
| `viewmodels/export_vm.py` | 导出改走后端适配器（单一代码路径）；导出后自动回归；导出记录新增 `backend` / `files` / `regression` / `regression_detail`；新增 `task_key()` / `backend()` / `export_options()` / `_regression_images()` |
| `views/export_tab.py` | 格式下拉按后端重建；导出记录表增加「回归」列（tooltip 显示明细） |
| `viewmodels/evaluate_vm.py` | 新增 `eval_views()`：视图集来自任务注册表 |
| `views/evaluate_tab.py` | 异常卡片与误检细分的显示条件改由 `eval_views()` 决定（删除结果里 `task == "anomaly"` 的硬编码） |

**导出产物**

| 后端 | 格式 | 产物 |
|------|------|------|
| YOLO | pt / onnx / torchscript | 单文件 |
| Anomalib | package | 目录：`model.ckpt` + `memory_bank.npz` + `config.json`（+ `backbone.onnx`，需 onnx 包） |

**回归结论语义**

| 状态 | 含义 |
|------|------|
| pass | 一致样本占比 ≥ 90% |
| fail | 低于阈值（需检查导出参数） |
| skip | 缺推理运行时或验证样本（附原因，不算失败） |

**已知限制（有意为之）**

- 异常检测模型包只做结构校验（清单 / 哈希 / 记忆库项），不做数值回归——包的推理需要部署端运行时（骨干 + 最近邻搜索），列入后续阶段。
- 回归取 8 张样本逐图对比，不是完整 mAP 复算；样本数与一致率都会写进导出记录。
- ONNX 数值回归需要 `onnxruntime`（当前环境未安装）：缺失时按 `skip` 处理并给出原因，不阻断导出。

**验证**：offscreen 自检 34 项 + 导出页定点自检 10 项全部通过。其中包含**真实导出链路的端到端验证**——`yolo11n.yaml` 随机初始化 → 保存 `.pt` → 导出 TorchScript → 源模型与导出模型对 3 张样本输出完全一致（`pass`）；模型包用桩模型验证了文件布局、清单哈希、篡改检测与适配器入口。

导出页定点自检另覆盖：格式下拉按后端重建（检测三项 / 异常一项）、**打开项目时回填下拉不把项目标记为已修改**（`_syncing` 守卫在嵌套调用中保持）、手动改格式正确写回项目配置。

### 8.12 实施记录（第 3 期 A：OCR 数据链路）

**新增文件**

| 文件 | 作用 |
|------|------|
| `services/ocr_metrics.py` | 识别质量指标：CER / WER / 精确匹配率（编辑距离，按语料汇总） |
| `services/backends/ocr.py` | PaddleOCR 后端：环境探测、**训练入口拦截**（不回落 YOLO）、子模型候选 det / rec |

**改动文件**

| 文件 | 改动 |
|------|------|
| `models/annotation.py` | 新增 `TEXT` 类型与 `text` 字段；`is_rect` / `center` / `set_center` 辅助（文本框与矩形同坐标口径） |
| `services/annotation_service.py` | 文本框转写**旁路存储**：`<标签目录>/texts/<图片主干>.txt`（每行 `cx cy<TAB>文本`，按中心点绑定）；YOLO txt 仍是标准 5 字段 |
| `services/dataset_service.py` | 新增 `layout="ocr_det_rec"`：在检测结构上追加 `ocr/det/<split>.txt`（PaddleOCR 检测格式）+ `ocr/rec/<split>/` 裁切图 + `ocr/rec_gt_<split>.txt`；无转写的框只进检测集（计入 `ocr_skipped`） |
| `viewmodels/dataset_vm.py` | `split_layout()` 支持 OCR 布局 |
| `utils/tasks.py` | OCR 任务放开（`supported=True`、后端 `ocr`、指标 CER / 精确匹配率） |
| `views/dialogs/new_project_dialog.py` | 详情面板「训练模型」按任务后端显示（不再一律 YOLO11）；标注方式文案补「文本框 + 转写」 |
| `views/widgets/annotation_canvas.py` | 新增 `MODE_TEXT`：拖出文本框，落为 `TEXT` 项 |
| `views/annotate_tab.py` | 工具条按任务重建（OCR 项目 = 浏览 + 文本）；编辑区「文本」行（仅选中文本框时出现）；对象列表显示转写 |
| `viewmodels/annotate_vm.py` | 新增 `set_text()`（带撤销快照，与几何编辑同机制） |

**数据约定**

| 项 | 说明 |
|----|------|
| 转写存储 | `labels/texts/<主干>.txt`，锚点为**归一化中心点**；框被移动或删除后该条转写失效（不会串到别的框上） |
| 检测标签 | `images/<split>/<图>` + `[{"transcription", "points", "difficult"}]`（像素坐标四点框） |
| 识别数据 | 裁切图 + `rec_gt_<split>.txt`（`路径<TAB>文本`） |

**为什么训练没接**：`paddlepaddle` 在当前 Python 3.14 下**没有可用轮子**（`pip index versions paddlepaddle` 无匹配），因此 OCR 的训练 / 评估 / 导出无法在本环境实现与验证。`OcrBackend.validate()` 会明确拦截训练请求并指出缺什么——**不会静默回退到 YOLO**（那会用错误路径消耗用户数据）。导出的 det / rec 数据集可直接交给外部 PaddleOCR 环境使用。

**验证**：offscreen 自检 36 项全部通过——任务与后端门控、契约往返、文本落盘与回读（YOLO txt 仍为标准 5 字段）、无文本时清理旁路文件、OCR 产物（检测标签指向划分后图片、裁切尺寸按框计算、识别集只含有转写的框）、指标数值、工具条随任务切换。

**顺带修掉的问题**：裁切边界原用 `int()` 截断，归一化坐标乘回像素的浮点误差（11.9999）会让裁切框少 1 像素，已改为四舍五入并夹到图内。

### 8.13 实施记录（第 4 期 A：U-Net 语义分割后端）

这是**第三个真正跑通的后端**，也是「新增一种模型类型」成本的实证：除了模型本身，其余环节只加了注册与布局两处。

**新增文件**

| 文件 | 作用 |
|------|------|
| `services/worker_utils.py` | 训练子进程公共工具：事件输出 / UTF-8 标准输出 / 轮边界暂停（`train_worker`、`anomaly_worker` 改为从这里导入，三份重复实现收敛为一份） |
| `services/segmentation_metrics.py` | 逐类 IoU / Dice / mIoU / 像素准确率（混淆矩阵口径；背景计入像素准确率但不计入 mIoU 均值） |
| `services/unet_model.py` | 轻量 U-Net（编解码各三层 + 瓶颈，纯 `torch.nn`）+ 检查点读写（权重 + 结构元信息） |
| `services/unet_worker.py` | 训练子进程：读 `images/ · masks/` 数据集、CE 损失、每轮算 mIoU / Dice、写 `best.pt` / `last.pt` / `results.csv`、支持暂停 |
| `services/backends/unet.py` | U-Net 适配器：环境探测（只需 torch）、数据校验、命令行、变体候选、暂停支持 |

**改动文件**

| 文件 | 改动 |
|------|------|
| `services/dataset_service.py` | 新增 `layout="mask"`：`images/{split}/` + `masks/{split}/*.png`（像素值 = cls_id + 1，0 为背景）；新增 `image_size()` 与 `mask_from_annotation()`（矩形 / 多边形 / 掩码栅格化，文本框不属于分割目标） |
| `viewmodels/dataset_vm.py` | `split_layout()` 支持掩码布局 |
| `utils/tasks.py` | 语义分割放开（`supported=True`、后端 `unet`、布局 `mask`、指标 mIoU / Dice、视图 `segment`） |
| `services/backends/__init__.py` | 注册 U-Net 后端（第 4 个） |

**数据约定**

| 项 | 说明 |
|----|------|
| 掩码 | 单通道 PNG，`0` = 背景，`N` = 第 N 类（`cls_id + 1`）；`masks/<split>/<图主干>.png` |
| 产物 | `<project_dir>/weights/best.pt`、`last.pt`、`results.csv`（每轮 `train/loss`、`val/loss`、`metrics/mIoU`、`metrics/Dice`） |
| 检查点 | 内含 `meta`（变体、类别数、输入尺寸），推理 / 导出据此重建网络 |

**已知限制（有意为之）**

- 语义分割的**评估卡与导出**未接入（第 4 期 B）：训练 / 产物 / 曲线已通，评估页暂不显示掩码对比。
- U-Net 是轻量实现（非 SOTA），定位是「平台能跑通第三种模型形态」与中小数据集基线。

**验证**：offscreen 自检 30 项全部通过，其中包含**真实训练链路**——4 张 64×48 图（多边形 + 矩形标注）→ 生成掩码数据集 → 用 `TrainService.build_args` 的命令行启动 `unet_worker` 子进程训练 2 轮 → 事件流含 `phase / iteration / epoch / done`、每轮带 mIoU 与损失、`best.pt` / `results.csv` 正确生成、检查点可回读（变体与类别数一致）、产物清单可被归档、模型前向输出形状正确；掩码像素值（背景 0 / 类别 1 / 类别 2）与指标算法（完美预测 mIoU = 1、全背景 = 0）也逐项核对。

### 8.14 实施记录（第 4 期 B：语义分割评估与导出）

**新增文件**

| 文件 | 作用 |
|------|------|
| `services/segmentation_service.py` | 语义分割共用服务：`pairs_for()`（图 / 掩码配对）、`load_model()`（`.pt` 与 `.torchscript` 通用）、`predict_mask()`（回到原图尺寸）、`evaluate_checkpoint()`（逐图预测 + 像素级指标 + 叠加对比图） |

**改动文件**

| 文件 | 改动 |
|------|------|
| `models/prediction.py` | 契约新增 `mask`（语义分割的预测掩码路径） |
| `utils/image_ops.py` | 新增 `overlay_mask()`（类别掩码叠加到原图）与 `mask_color()` |
| `services/backends/unet.py` | 新增 `predict()`（掩码落盘供回归比对）与 `export()`（TorchScript + `<name>.meta.json` 记录输入尺寸） |
| `services/regression_service.py` | 新增 `same_mask()`：语义分割的回归口径为**掩码像素一致率 ≥ 99%** |
| `services/unet_worker.py` | `split_pairs()` 改为复用 `SegmentationService.pairs_for()`（训练与评估同一套配对规则） |
| `viewmodels/evaluate_vm.py` | 评估分派新增语义分割分支；新增 `_run_segmentation_evaluation()` / `_segmentation_pairs()` / `_sample_pairs()`；完成提示改报 mIoU；`_recompute()` 对语义分割短路（像素级指标不能由图级行记录重算） |
| `views/evaluate_tab.py` | 新增「语义分割」卡片（mIoU / Dice / 像素准确率 + 逐类 IoU 表）；语义分割下图级指标与混淆矩阵置空 |

**数据与口径**

| 项 | 说明 |
|----|------|
| 评估输入 | 拆分产物的 `images/<subset>` + `masks/<subset>`（或自定义目录同结构），支持多子集与随机抽样 |
| 指标 | 像素级混淆矩阵 → mIoU / Dice / 像素准确率；背景计入像素准确率但不计入 mIoU 均值 |
| 结果行 | `label` / `pred_label` 取掩码主导类别，`confidence` 为像素一致率，`annotated` 为叠加对比图 |
| 导出 | `.torchscript` + `.meta.json`（`imgsz` / `num_classes`），回归与部署都靠后者还原输入尺寸 |

**已知限制（有意为之）**：混淆矩阵的「点击单元格筛选图像」对像素级矩阵不适用，因此语义分割下不显示矩阵（改用逐类 IoU 表）；U-Net 仍是轻量实现。

**验证**：offscreen 自检 24 项全部通过——6 张 64×48 图 → 掩码拆分 → 训练 1 轮 → 评估服务（指标齐全、逐类名称、3×3 像素矩阵、结果行字段、叠加图文件落盘）→ TorchScript 导出（单文件 + 元信息 + 产物清单）→ **回归**（源 `.pt` 与导出件掩码一致 `pass`；篡改掩码 → 不一致；缺掩码 → 不一致）→ 评估页卡片（填充 mIoU 与 3 行逐类表、图级指标置空、检测结果不显示语义卡片）。

### 8.15 实施记录（第 4 期 C：训练设置归档与多路线对比）

**改动的两个文件**

| 文件 | 改动 |
|------|------|
| `viewmodels/train_vm.py` | 训练完成时把当前设置的 `setup_id` / `setup_name` 写进记录；产物归档虚拟路径由 `runs/<文件名>` 改为 `runs/<保存目录名>-<时间戳>/<文件名>`；记录里回写 `archive_folder` 便于回溯；新增 `_run_folder_name()` / `_safe_name()` |
| `views/train_tab.py` | 左侧训练记录、对比页的模型选择表（4 → 5 列）、指标对比表（6 → 7 列）增加「设置」；对比曲线的图例也带上设置名；**单套设置下不显示**这些标注 |

**设计取舍**

| 项 | 说明 |
|----|------|
| 为什么给产物加时间戳 | 异常检测与语义分割的输出目录由配置固定（`runs/anomaly`、`runs/unet`），此前归档路径固定为 `runs/best.pt`，第二次训练会顶掉第一次的归档 |
| 为什么单套设置不下显示 | 多数项目只有一套设置，此时「设置 1」是纯噪音；仅在多套时标注归属 |
| 未做的部分 | 「按 setup 过滤训练记录」未做——对比页勾选任意两条已能完成两两比较；跨任务范式共处一个项目仍未支持（项目仍绑定单一任务） |

**顺带修掉的问题**：归档目录名基于「秒级时间戳」，同一秒内完成两次训练会撞名；现在检测到重复时自动追加序号（同时把 `archive_folder` 记进训练记录）。

**验证**：offscreen 自检 14 项全部通过——目录名生成与非法字符替换、空名回退；训练完成写入 setup 归属、产物归档到独立目录、第二次训练（新设置）记录指向新设置且归档路径不同、记录里留有归档目录；界面上左侧记录标注 setup、对比选择表 5 列且设置列正确、指标对比表 7 列表头为「时间 / 拆分 / 设置 / 模型 / 最佳指标 / 最佳轮次 / 最终 Loss」。
