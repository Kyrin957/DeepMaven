# 基于PySide6的深度学习缺陷检测系统——开发方案与实施步骤


## 一、项目概述

### 1.1 目标定位

参考Halcon Deep Learning Tool的设计理念，开发一款面向工业缺陷检测（划痕、异物等）的桌面应用，实现从项目管理、数据管理、模型训练到模型导出的完整工作流。

### 1.2 核心功能模块

| 模块 | 功能描述 |
|------|---------|
| **项目管理** | 创建/打开/保存项目，管理项目文件结构 |
| **数据管理** | 数据集导入、标注、预览、划分（训练/验证/测试集） |
| **预训练模型导入** | 导入YOLO系列预训练权重，支持模型选择与配置 |
| **模型训练** | 参数配置、训练启动、进度监控、指标可视化 |
| **模型导出** | 导出训练好的模型（ONNX等格式），便于部署 |


## 二、技术选型

### 2.1 核心框架

| 组件 | 技术选型 | 说明 |
|------|---------|------|
| GUI框架 | **PySide6** | Qt for Python官方绑定，功能完善、跨平台 |
| 深度学习框架 | **PyTorch + Ultralytics** | YOLO系列模型训练与推理的首选方案 |
| 目标检测模型 | **YOLOv11/YOLOv26** | 工业缺陷检测主流算法，支持检测、分割等任务 |
| 图像处理 | **OpenCV-Python** | 图像读取、预处理、标注可视化 |
| 数据存储 | **SQLite** | 轻量级本地数据库，存储项目信息和训练记录 |
| 模型导出 | **PT/ONNX** | 跨平台模型部署格式 |

### 2.2 开发环境

- Python 3.14+
- PySide6 >= 6.11
- PyTorch （CUDA可选）
- Ultralytics 

### 2.3 可选增强

- **QFluentWidgets**：Fluent Design风格组件库，提升界面美观度
- **Matplotlib**：训练曲线可视化


## 三、软件架构设计

### 3.1 整体架构（MVVM模式）

MVVM架构设计：

```
src/
├── models/          # Model层 - 数据模型与状态
│   ├── project.py   # 项目数据模型
│   ├── dataset.py   # 数据集模型
│   └── training.py  # 训练配置模型
├── viewmodels/      # ViewModel层 - 业务逻辑
│   ├── project_vm.py
│   ├── dataset_vm.py
│   ├── train_vm.py
│   └── export_vm.py
├── views/           # View层 - UI界面
│   ├── main_window.py
│   ├── project_tab.py
│   ├── data_tab.py
│   ├── train_tab.py
│   └── export_tab.py
├── services/        # Service层 - 外部服务
│   ├── yolo_service.py     # YOLO训练/推理封装
│   ├── dataset_service.py  # 数据集处理
│   └── export_service.py   # 模型导出
└── utils/           # 工具类
    ├── logger.py
    ├── config.py
    └── constants.py
```

### 3.2 各层职责

- **Model层**：定义数据结构（项目信息、数据集元数据、训练参数等）
- **ViewModel层**：处理业务逻辑，连接Model和View，响应UI事件
- **View层**：PySide6界面组件，响应用户操作，展示数据
- **Service层**：封装YOLO训练、数据集处理、模型导出等核心功能


## 四、界面布局设计

参考已有系统的功能布局，建议采用**左侧导航栏+右侧内容区**的经典布局：

### 4.1 导航标签页

| 标签页 | 核心功能 |
|-------|---------|
| **项目管理** | 新建项目、打开项目、项目信息展示、最近项目列表 |
| **数据管理** | 数据集导入、图片预览、标注查看、数据集划分、统计信息 |
| **模型管理** | 预训练模型选择（YOLOv11n/s/m/l/x）、权重导入、模型信息展示 |
| **模型训练** | 训练参数配置（epochs、batch size、learning rate、image size）、启动/停止训练、进度条、损失曲线、mAP等指标实时显示 |
| **模型导出** | 格式选择（PT/ONNX）、导出路径设置、导出执行 |
| **模型评估** | 图片/相机输入、实时检测、结果展示与导出 |

### 4.2 界面风格

推荐使用**QFluentWidgets**实现Fluent Design风格，支持亮暗主题切换。


## 五、分步实施计划

### 第一阶段：基础框架搭建（第1-2周）

**目标**：搭建可运行的应用骨架

1. **环境搭建**
   - 安装Python 3.14+、PySide6、PyTorch、Ultralytics
   - 配置虚拟环境，编写requirements.txt

2. **项目骨架**
   - 按MVVM架构创建目录结构
   - 实现主窗口（QMainWindow）和左侧导航栏
   - 实现信号槽机制和事件处理

3. **项目管理模块（基础）**
   - 定义项目数据模型（项目名称、创建时间、路径、模型类型等）
   - 实现新建项目、打开项目、保存项目功能
   - 使用SQLite存储项目元数据

### 第二阶段：数据管理模块（第3-4周）

**目标**：实现数据集的导入、预览和管理

1. **数据集导入**
   - 支持图片文件夹导入
   - 支持YOLO格式标签（images/ + labels/ 结构）
   - 支持COCO等格式转换

2. **数据预览与标注查看**
   - 图片缩略图网格展示
   - 图片详情查看（带标注框叠加显示）
   - 缺陷类别统计

3. **数据集划分**
   - 训练集/验证集/测试集按比例自动划分
   - 支持自定义划分

### 第三阶段：预训练模型导入（第5周）

**目标**：实现预训练模型的导入与管理

1. **模型选择**
   - 下拉菜单选择YOLO模型变体（YOLO11 / YOLO26 n/s/m/l 等）
   - 显示模型参数信息（参数量、FLOPs等）

2. **权重导入**
   - 支持导入官方预训练权重（自动下载）
   - 支持导入自定义预训练权重（.pt文件选择）
   - 模型初始化与验证

### 第四阶段：模型训练模块（第6-8周）

**目标**：实现完整的模型训练工作流

1. **训练参数配置**
   - 任务类型：检测/分类/分割
   - 训练轮数（epochs）、批次大小（batch size）
   - 学习率、优化器选择
   - 图像尺寸（imgsz）

2. **训练执行与监控**
   - 启动/暂停/停止训练
   - 进度条实时更新
   - 损失曲线动态绘制（使用Matplotlib）
   - mAP、Precision、Recall等指标实时显示

3. **训练日志**
   - 训练过程日志输出
   - 训练结果自动保存（best.pt / last.pt）

### 第五阶段：模型评估模块（第10-11周）

**目标**：实现模型加载与缺陷检测

1. **推理引擎**
   - 加载训练好的模型
   - 支持图片检测、视频检测、相机实时检测
   - 置信度和IOU阈值调节

2. **结果展示**
   - 检测框叠加显示
   - 缺陷类别、置信度标注
   - 检测结果统计（耗时、目标数量、位置信息）

3. **结果导出**
   - 检测结果图片导出
   - 检测报告导出（CSV/Excel）

### 第五阶段：模型导出模块（第9周）

**目标**：实现训练后模型的导出

1. **格式支持**
   - PyTorch格式（.pt）导出
   - ONNX格式导出
   - 支持导出为TorchScript

2. **导出配置**
   - 选择导出格式
   - 设置导出路径
   - 导出进度显示

### 第七阶段：测试与打包（第12周）

**目标**：完善测试并进行应用打包

1. **功能测试**
   - 各模块功能完整性测试
   - 异常处理测试
   - 跨平台兼容性测试（Windows/Linux/macOS）

2. **性能优化**
   - 大数据集加载优化
   - GPU推理加速
   - 多线程处理

3. **应用打包**
   - 使用PyInstaller打包为可执行文件
   - 使用pyside6-deploy部署


## 六、关键实现要点

### 6.1 YOLO训练服务封装

```python
# services/yolo_service.py
from ultralytics import YOLO

class YOLOService:
    def __init__(self, model_name="yolov8n.pt"):
        self.model = YOLO(model_name)
    
    def train(self, data_yaml, epochs, batch, imgsz, lr):
        results = self.model.train(
            data=data_yaml,
            epochs=epochs,
            batch=batch,
            imgsz=imgsz,
            lr0=lr
        )
        return results
    
    def export(self, format="onnx"):
        self.model.export(format=format)
```

### 6.2 数据集结构要求

YOLO格式数据集结构：
```
dataset/
├── images/
│   ├── train/
│   └── val/
├── labels/
│   ├── train/
│   └── val/
└── data.yaml
```

### 6.3 多线程处理

训练和推理为耗时操作，需使用**QThread**或**QRunnable**在后台执行，避免UI卡顿。


## 七、风险与应对

| 风险 | 应对措施 |
|------|---------|
| GPU环境配置复杂 | 提供详细的环境配置文档，支持CPU模式降级运行 |
| 训练过程耗时长 | 实现训练状态保存与恢复（checkpoint），支持断点续训 |
| 大数据集加载慢 | 使用懒加载和分页加载，优化内存管理 |
| 跨平台兼容性 | 尽早进行跨平台测试，使用条件编译处理平台差异 |
